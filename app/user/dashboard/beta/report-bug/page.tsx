import UserDashboardShell,{DashboardCard} from "@/components/user/UserDashboardShell";import { getCurrentUserDashboardContext } from "@/lib/user-dashboard";import BugForm from "./BugForm";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const ctx=await getCurrentUserDashboardContext();const sp=await searchParams;return <UserDashboardShell isBeta={ctx.isBeta}><DashboardCard><h1 className="text-4xl font-black">Report a Bug</h1><p className="mt-2 text-white/60">Share what happened, what you expected, and how to reproduce it.</p><BugForm defaults={sp}/></DashboardCard></UserDashboardShell>}
