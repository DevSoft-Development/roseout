import Link from 'next/link';
import CrmWorkspaceShell from '@/components/admin/crm/CrmWorkspaceShell';
import { requireAdminRole } from '@theouthaven/auth/admin-session';
import { CRM_READ_ROLES } from '@/lib/crm/permissions';
import { getGtmTerritorySummary } from '@/lib/gtm/territories';
export const dynamic='force-dynamic';
export default async function GtmTerritoriesPage(){await requireAdminRole(CRM_READ_ROLES); const rows=await getGtmTerritorySummary(); return <CrmWorkspaceShell><main className="space-y-5 text-[var(--admin-shell-text)]"><header className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.2em] text-rose-300">GTM Territories</p><h1 className="mt-1 text-3xl font-black">Neighborhood Sales Coverage</h1><p className="mt-1 text-[var(--admin-shell-muted)]">Market → borough/county → territory → neighborhoods → businesses.</p></div><Link href="/admin/dashboard/crm/gtm" className="rounded-xl border border-[var(--admin-shell-border)] px-4 py-2 text-sm font-black">Revenue Intelligence</Link></header><section className="grid gap-4 xl:grid-cols-2">{rows.map((r:any)=><article key={r.id} className="rounded-3xl border border-[var(--admin-shell-border)] bg-[var(--admin-shell-card)] p-5"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-black">{r.name}</h2><p className="mt-1 text-sm text-[var(--admin-shell-muted)]">{r.borough||'Market'} · {r.neighborhoods.join(', ')||'Neighborhoods not assigned'}</p></div><b className="text-2xl">{r.avgOpportunity}</b></div><div className="mt-4 grid grid-cols-3 gap-2 sm:grid-cols-6">{[['Businesses',r.searchable],['Full CRM',r.full],['Hot',r.hot],['Warm',r.warm],['Engaged',r.engaged],['Customers',r.customers]].map(([a,b])=><div key={String(a)} className="rounded-xl bg-[var(--admin-shell-soft)] p-3"><b>{b}</b><small className="block text-[10px] text-[var(--admin-shell-muted)]">{a}</small></div>)}</div><p className="mt-4 text-xs text-[var(--admin-shell-muted)]">Average demand: {r.avgDemand}</p></article>)}</section>{!rows.length?<p className="rounded-3xl border border-[var(--admin-shell-border)] bg-[var(--admin-shell-card)] p-5 text-sm text-[var(--admin-shell-muted)]">No active CRM territories are configured yet.</p>:null}</main></CrmWorkspaceShell>}
