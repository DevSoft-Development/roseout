"use client";import{useState}from"react";import Link from"next/link";export default function SupportClient({tickets}:{tickets:any[]}){const[rows,setRows]=useState(tickets);const[m,setM]=useState("");const[f,setF]=useState({subject:"",category:"Account",message:""});async function submit(e:any){e.preventDefault();const r=await fetch('/api/user/support/tickets',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const j=await r.json();if(j.success){setRows([j.ticket,...rows]);setF({subject:"",category:"Account",message:""});setM("Ticket created.")}else setM(j.error||"Could not create ticket.")}return <div className="grid gap-5 lg:grid-cols-2"><section className="rounded-3xl border border-white/10 bg-white/[.04] p-5"><h2 className="text-2xl font-black">Create support ticket</h2><form onSubmit={submit} className="mt-4 grid gap-3"><input className="rounded-2xl bg-black/30 p-3" placeholder="Subject" value={f.subject} onChange={e=>setF({...f,subject:e.target.value})}/><select className="rounded-2xl bg-black/30 p-3" value={f.category} onChange={e=>setF({...f,category:e.target.value})}>{['Account','Saved outing','Booking/reservation','Bug','Billing/plan','Other'].map(x=><option key={x}>{x}</option>)}</select><textarea className="min-h-32 rounded-2xl bg-black/30 p-3" placeholder="How can we help?" value={f.message} onChange={e=>setF({...f,message:e.target.value})}/><button className="rounded-full bg-rose-600 px-5 py-3 font-black">Submit ticket</button>{m&&<p className="text-sm text-rose-100">{m}</p>}</form></section><section className="rounded-3xl border border-white/10 bg-white/[.04] p-5"><h2 className="text-2xl font-black">Your tickets</h2><div className="mt-4 grid gap-3">{rows.length?rows.map(t=><Link key={t.id} href={`/user/dashboard/support/${t.id}`} className="rounded-2xl border border-white/10 bg-black/20 p-4"><b>{t.subject}</b><p className="text-sm text-white/50">{t.status} · {t.category||'Other'} · {new Date(t.updated_at||t.created_at).toLocaleDateString()}</p></Link>):<p className="text-white/55">No support tickets yet.</p>}</div></section></div>}
