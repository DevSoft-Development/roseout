export function compareSearchResults(legacy: { success?: boolean; restaurants?: unknown[]; activities?: unknown[]; pairs?: unknown[] }, v2: { success: boolean; counts: { displayedResults: number } }) { return { legacyFulfilled: Boolean(legacy.success), legacyDisplayed: (legacy.restaurants?.length ?? 0) + (legacy.activities?.length ?? 0) + (legacy.pairs?.length ?? 0), v2Fulfilled: v2.success, v2Displayed: v2.counts.displayedResults }; }
