import { requireAdminApiRole } from "@/lib/admin-api-auth";import { refreshLocationSearchProfile } from "@/lib/search/profile";import { NextResponse } from "next/server";
export async function POST(_request:Request,{params}:{params:Promise<{locationId:string}>}){const auth=await requireAdminApiRole(["superadmin","admin"]);if(auth.error)return auth.error;try{return NextResponse.json({profile:await refreshLocationSearchProfile((await params).locationId,"admin_rebuild")});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Rebuild failed"},{status:500});}}
