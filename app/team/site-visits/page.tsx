import Link from "next/link"; import TeamSiteVisitClient from "@/components/TeamSiteVisitClient"; import { ensureTeamProfileForCurrentUser } from "@/lib/team-tools"; import { supabaseAdmin } from "@/lib/supabase-admin";
export const dynamic="force-dynamic";
export default async function TeamSiteVisits(){ const {profile}=await ensureTeamProfileForCurrentUser(); if(!profile.can_do_site_visits) return <main className="min-h-screen bg-[#080808] p-6 text-white"><div className="mx-auto max-w-4xl"><Link href="/team" className="text-sm font-black text-rose-200">← Team</Link><h1 className="mt-6 text-3xl font-black">Site visits are not enabled for your team profile.</h1></div></main>; const {data:locations=[]}=await supabaseAdmin.from("locations").select("id,name,location_name,address,city,state,latitude,longitude").order("name").limit(200); return <main className="min-h-screen bg-[#080808] px-4 py-8 text-white"><div className="mx-auto max-w-5xl"><Link href="/team" className="text-sm font-black text-rose-200">← Team</Link><div className="mt-6"><TeamSiteVisitClient locations={locations || []}/></div></div></main> }
