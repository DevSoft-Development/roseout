"use client";
import { useState } from "react";
import type { AiTagHelperSettings } from "@/lib/ai-tag-helper-settings";
const opts=[['off','Off'],['admins_only','Admins Only'],['paid_only','Paid Locations Only'],['all','All Locations']];
export default function AiTagHelperSettingsClient({initial}:{initial:AiTagHelperSettings}){const[access,setAccess]=useState(initial.access);const[status,setStatus]=useState("");async function save(){setStatus("Saving…");const res=await fetch('/api/admin/settings/ai-tag-helper',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({access})});const data=await res.json().catch(()=>({}));setStatus(res.ok?'Saved AI Helper Setting.':data.error||'Could not save AI Helper Setting.')}return <section className="rounded-3xl border border-white/10 bg-[#120d0b] p-6"><h2 className="text-xl font-bold text-rose-100">AI Tag Helper</h2><p className="mt-2 text-sm text-white/70">Control who can generate suggested profile tags in the CRM and location dashboard. Suggestions require review and do not auto-save.</p><label className="mt-4 block text-sm font-bold text-white/70"><span>AI Tag Helper Access</span><select value={access} onChange={e=>setAccess(e.target.value as any)} className="mt-2 w-full rounded-2xl border border-white/10 bg-black/30 px-4 py-3 text-white">{opts.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><ul className="mt-3 list-disc pl-5 text-xs leading-5 text-white/50"><li>AI suggestions are drafts only.</li><li>AI does not overwrite owner/admin edits.</li><li>Review Keywords stay private and are generated from reviews only.</li></ul><button onClick={save} className="mt-4 rounded-full bg-rose-600 px-5 py-2.5 text-sm font-black text-white">Save AI Helper Setting</button>{status?<p className="mt-3 text-sm font-bold text-white/70">{status}</p>:null}</section>}
