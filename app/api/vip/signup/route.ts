import { NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/supabase-admin";
import { requireTurnstile } from "@/lib/security/turnstile";
import { trackGrowthProEvent } from "@/lib/growth-pro/analytics";
export async function POST(request: Request){const body=await request.json().catch(()=>({})); const check=await requireTurnstile({request,token:body.turnstileToken,action:"vip_signup"}); if(!check.success)return NextResponse.json({error:check.error},{status:check.status}); const locationId=String(body.locationId||""); if(!locationId)return NextResponse.json({error:"Please choose a location before submitting."},{status:400}); await supabaseAdmin.from("location_vip_signups").insert({location_id:locationId,name:body.name,email:body.email,phone:body.phone,birthday_month:body.birthdayMonth,interests:body.interests||[],source:body.source||"public_growth_pro",email_consent:Boolean(body.emailConsent||body.email),sms_consent:Boolean(body.smsConsent),consent_text:"Customer joined this location’s VIP list only.",consent_ip_hash:check.ipHash,consent_user_agent:request.headers.get("user-agent"),consent_at:new Date().toISOString()}); await trackGrowthProEvent(locationId,"vip_signup",{}); return NextResponse.json({message:"Thanks — you joined this location’s VIP list."});}
