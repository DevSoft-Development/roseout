import {describe,expect,it} from "vitest";import {buildUserPreferenceProfile,personalizationAdjustment} from "../personalization";
describe("privacy-safe personalization",()=>{const now=new Date("2030-01-01T00:00:00Z");it("isolates users, ignores cold start and decays old evidence",()=>{const events=[{userId:"a",type:"save" as const,occurredAt:"2029-12-31T00:00:00Z",cuisine:"Italian"},{userId:"b",type:"completed" as const,occurredAt:"2029-12-31T00:00:00Z",cuisine:"Thai"}];const p=buildUserPreferenceProfile("a",events,now);expect(p.cuisines.thai).toBeUndefined();expect(personalizationAdjustment(p,{cuisine:"Italian"})).toBeGreaterThan(0);expect(buildUserPreferenceProfile("a",[],now).evidence).toBe(0);});it("caps boosts and explicit intent overrides history",()=>{const p=buildUserPreferenceProfile("a",Array.from({length:20},(_,i)=>({userId:"a",type:"completed" as const,occurredAt:`2029-12-${String(10+i%10).padStart(2,"0")}T00:00:00Z`,cuisine:"Italian"})),now);expect(personalizationAdjustment(p,{cuisine:"Italian"})).toBe(8);expect(personalizationAdjustment(p,{cuisine:"Italian"},["thai"])).toBe(0);});});
