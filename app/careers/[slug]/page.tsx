import Link from "next/link"; import { notFound } from "next/navigation"; import { getPublicCareerJobBySlug } from "@/lib/careers/queries"; import { getCompensationLabel, getInternshipTypeLabel, requiresInternshipComplianceChecklist } from "@/lib/careers/format";
export default async function CareerDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const job:any = await getPublicCareerJobBySlug(slug); if (!job) notFound(); const sections = [["Role Overview", job.overview || job.summary], ["What You’ll Do", job.responsibilities], ["Who You Are", job.requirements], ["Requirements", job.requirements], ["Nice to Have", job.nice_to_have], ["Benefits", job.benefits], ["Schedule", job.schedule], ["Location", job.location], ["Hiring Process", job.hiring_process || "Applications are reviewed by TheOutHaven. Qualified candidates may be invited to a portfolio review, interview, content test, or offer conversation."]]; return <main className="min-h-screen bg-[#050505] text-white"><section className="mx-auto max-w-5xl px-4 py-14"><Link className="text-sm font-bold text-rose-200" href="/careers">← All careers</Link><h1 className="mt-5 text-4xl font-black">{job.title}</h1><p className="mt-3 text-white/60">{job.department} · {job.location || "Remote"} · {job.employment_type}</p><p className="mt-3 font-black text-rose-100">{getCompensationLabel(job)}</p>{job.is_internship?<p className="mt-3 inline-flex rounded-full border border-white/10 px-3 py-1 text-xs font-black">{getInternshipTypeLabel(job.internship_type)}</p>:null}{requiresInternshipComplianceChecklist(job)?<div className="mt-6 rounded-2xl border border-amber-300/20 bg-amber-500/10 p-4 text-sm leading-6 text-amber-50">This internship is structured as an educational experience. The role is designed to help interns learn marketing, social media planning, content strategy, brand research, and startup operations. Educational and college-credit internships are reviewed before posting and may require school documentation.</div>:null}<Link href={`/careers/${job.slug}/apply`} className="mt-7 inline-flex rounded-xl bg-[#ec0b5b] px-5 py-3 text-sm font-black">Apply Now</Link><div className="mt-10 grid gap-4">{sections.filter(([,v])=>v).map(([t,v])=><section className="rounded-3xl border border-white/10 bg-white/[0.045] p-5" key={t}><h2 className="text-xl font-black">{t}</h2><p className="mt-3 whitespace-pre-line text-sm leading-7 text-white/65">{v}</p></section>)}</div></section></main> }
