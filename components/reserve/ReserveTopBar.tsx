"use client";
import { ReactNode } from "react";
export default function ReserveTopBar({actions,themeToggle,setupEnabled}:{actions?:ReactNode;themeToggle?:ReactNode;setupEnabled?:boolean}){return <header className="reserve-card mb-5 rounded-[2rem] p-4 sm:p-6"><div className="flex flex-col gap-4 xl:flex-row xl:items-center xl:justify-between"><div><div className="flex flex-wrap items-center gap-2"><p className="text-xs font-black uppercase tracking-[.24em] reserve-muted">Reserve operations</p><span className={`rounded-full px-3 py-1 text-xs font-black ${setupEnabled?'bg-green-500/15 text-green-400':'bg-amber-500/15 text-amber-500'}`}>{setupEnabled?'Live setup enabled':'Setup needs attention'}</span></div><h1 className="mt-2 text-3xl font-black sm:text-4xl">Reserve Command Center</h1><p className="mt-2 reserve-muted">Run today’s bookings, floor, waitlist, and guest flow from one screen.</p></div><div className="flex flex-wrap gap-2">{actions}{themeToggle}</div></div></header>}
