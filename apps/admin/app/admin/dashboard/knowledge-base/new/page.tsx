import { requireAdminRole } from "@theouthaven/auth/admin-session";import { getKbCategories } from "@/lib/knowledge-base/server";import { roleCanEditKb } from "@/lib/knowledge-base/access";import { redirect } from "next/navigation";import ArticleForm from "../ArticleForm";import KbTabs from "../KbTabs";export default async function Page(){const admin=await requireAdminRole(["superadmin","admin","editor"]);if(!roleCanEditKb(admin.role))redirect("/admin/unauthorized");const categories=await getKbCategories();return <main className="min-h-screen bg-[#08050b] p-6 text-white"><div className="mx-auto max-w-5xl space-y-6"><h1 className="text-4xl font-black">New Knowledge Base Article</h1><KbTabs/><ArticleForm categories={categories}/></div></main>}
