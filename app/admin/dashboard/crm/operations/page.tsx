import CrmWorkspaceShell from "@/components/admin/crm/CrmWorkspaceShell";import { requireAdminRole } from "@/lib/admin-auth";import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions";import { operationsSnapshot } from "@/lib/crm/core-modules";export const dynamic="force-dynamic";export default async function Page(){await requireAdminRole(ADMIN_PAGE_ACCESS.crm);const s=await operationsSnapshot();const groups=[['Publishability',s.hidden,'Recompute publishability'],['Claims',s.claims,'Reassign or resend claim invitation'],['Support',s.support,'Escalate or rebalance case'],['Onboarding / Tasks',s.tasks,'Requeue task owner'],['Claim codes',s.codes,'Rebuild or expire code']];const critical=(s.hidden.count||0)+(s.claims.count||0);return <CrmWorkspaceShell><main className="space-y-5 text-white"><header><p className="text-xs font-black uppercase tracking-[.25em] text-rose-300">Operations</p><h1 className="text-3xl font-black">Exception and repair center</h1><p className="text-white/60">Real exceptions from publishability, claims, onboarding, support, billing/reservation-adjacent work, and system queues when available.</p></header><section className="grid gap-3 md:grid-cols-5">{[['Critical blockers',critical],['Attention needed',(s.support.count||0)+(s.tasks.count||0)],['SLA risk',s.claims.count||0],['Unassigned',s.tasks.data?.filter((x:any)=>!x.assigned_to_user_id).length||0],['Resolved this week',0]].map(([k,v])=><article className="rounded-2xl border border-white/10 bg-white/[.04] p-4" key={k}><p className="text-xs uppercase text-white/45">{k}</p><b className="text-2xl">{v}</b></article>)}</section><section className="grid gap-4">{groups.map(([name,res,repair]:any)=><article key={name} className="rounded-2xl border border-white/10 bg-white/[.04] p-4"><h2 className="text-xl font-black">{name} <span className="text-sm text-white/40">{res.count??res.data?.length??0}</span></h2><div className="mt-3 overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead><tr className="text-xs uppercase text-white/45"><th>Severity</th><th>Affected record</th><th>Explanation</th><th>Owner</th><th>Age</th><th>Status</th><th>Action</th></tr></thead><tbody>{(res.data||[]).map((x:any)=><tr className="border-t border-white/10" key={x.id}><td className="py-3">{name==='Publishability'?'critical':'attention'}</td><td>{x.name||x.subject||x.title||x.claim_code||x.id}</td><td>{repair}</td><td>{x.assigned_to_user_id||x.assigned_to||'System'}</td><td>{x.updated_at?new Date(x.updated_at).toLocaleDateString():'—'}</td><td>{x.status||x.claim_status||'open'}</td><td><button className="rounded bg-white/10 px-3 py-1">Run safe repair</button></td></tr>)}</tbody></table></div></article>)}</section></main></CrmWorkspaceShell>}
