"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
function locName(l:any){return l.display_name||l.name||l.location_name||l.restaurant_name||l.activity_name||"Untitled location"}
export default function TeamSocialOutreachClient({ locations, templates, isAdmin=false }: { locations:any[]; templates:any[]; isAdmin?:boolean }) { const [template,setTemplate]=useState<any>(templates[0]); const [msg,setMsg]=useState(""); const [q,setQ]=useState(""); const [results,setResults]=useState<any[]>(locations.slice(0,10)); const [selected,setSelected]=useState<any>(locations[0]||null); const [handle,setHandle]=useState(""); useEffect(()=>{const t=setTimeout(async()=>{if(q.trim().length<2){setResults(locations.slice(0,10));return} const r=await fetch(`/api/team/location-search?q=${encodeURIComponent(q)}&limit=15`); const d=await r.json(); setResults(d.locations||[]);},250); return()=>clearTimeout(t)},[q,locations]); function choose(l:any){setSelected(l); setHandle(l.owner_instagram||l.instagram||"");} async function submit(fd:FormData){ if(selected) fd.set("locationId", selected.id); fd.set("handleOrUrl", handle); setMsg("Saving outreach..."); const res=await fetch("/api/team/social-outreach",{method:"POST",body:fd}); const data=await res.json(); setMsg(res.ok?"Outreach saved for review.":data.error||"Could not save outreach."); } return <form action={submit} className="rounded-[2rem] border border-white/10 bg-[#111] p-5 sm:p-7"><h1 className="text-3xl font-black">My Social Outreach</h1><p className="mt-2 text-sm font-bold text-white/55">Log Partner Launch social outreach for permitted workspace locations.</p><section className="mt-5 rounded-3xl border border-rose-300/15 bg-black/30 p-4"><label className="text-sm font-black text-rose-100">Search locations inline<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Business, address, phone, neighborhood, borough, city, category" className="mt-2 w-full rounded-2xl border border-white/10 bg-black px-4 py-3 text-white outline-none"/></label><div className="mt-3 grid gap-3 md:grid-cols-2">{results.map(l=><button type="button" key={l.id} onClick={()=>choose(l)} className={`rounded-2xl border p-4 text-left ${selected?.id===l.id?"border-rose-300/60 bg-rose-500/15":"border-white/10 bg-white/[0.04]"}`}><p className="font-black text-white">{locName(l)}</p><p className="mt-1 text-xs text-white/55">{l.address||"No address"}</p><p className="mt-1 text-xs text-white/45">{l.display_phone||l.phone||"No phone"} · {l.display_category||l.category||l.location_type||"category n/a"}</p><p className="mt-2 text-[11px] font-bold text-rose-100">Claim: {l.claim_status||l.claim_outreach_status||"—"} · Partner: {l.partner_sales_status||"—"}</p></button>)}</div>{selected?<div className="mt-3 rounded-2xl border border-white/10 bg-black/40 p-3 text-sm text-white/65">Selected: <b className="text-white">{locName(selected)}</b>{isAdmin?<Link className="ml-3 text-rose-200 underline" href={`/admin/dashboard/crm/${selected.id}`}>Open admin CRM</Link>:null}</div>:null}</section><input type="hidden" name="locationId" value={selected?.id||""}/><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="text-sm font-bold text-white/65">Platform<select name="platform" className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{["instagram","facebook","tiktok","x_twitter","linkedin","threads","youtube","pinterest","yelp","google_business_profile","tripadvisor","website_contact_form","other"].map(p=><option key={p}>{p}</option>)}</select></label><label className="text-sm font-bold text-white/65">Handle or URL<input value={handle} onChange={e=>setHandle(e.target.value)} name="handleOrUrl" placeholder="@handle or profile URL" className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white"/></label><label className="text-sm font-bold text-white/65">Template<select name="templateId" value={template?.id||""} onChange={(e)=>setTemplate(templates.find(t=>t.id===e.target.value))} className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{templates.map(t=><option key={t.id} value={t.id}>{t.template_name}</option>)}</select><input type="hidden" name="templateVersion" value={template?.version||1}/></label><label className="text-sm font-bold text-white/65">Message status<select name="messageStatus" className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{["not_sent","sent","failed","unable_to_message","profile_private","profile_inactive","wrong_profile","needs_follow_up","do_not_contact"].map(v=><option key={v}>{v}</option>)}</select></label><label className="text-sm font-bold text-white/65">Outreach stage<select name="outreachStage" className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{["profile_found","message_sent","follow_up_needed","replied","interested","not_interested","claim_code_sent","onboarding_started","onboarded","do_not_contact","needs_manager_review","closed"].map(v=><option key={v}>{v}</option>)}</select></label><label className="text-sm font-bold text-white/65">Reply status<select name="replyStatus" className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{["no_reply","positive_reply","negative_reply","asked_question","requested_call","requested_email","requested_pricing","requested_demo","blocked_or_unavailable"].map(v=><option key={v}>{v}</option>)}</select></label><label className="text-sm font-bold text-white/65">Follow-up at<input name="followUpAt" type="datetime-local" className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white"/></label><label className="text-sm font-bold text-white/65">Screenshot/proof<input name="proof" type="file" accept="image/*" className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white"/></label><label className="text-sm font-bold text-white/65 md:col-span-2">Notes<textarea name="notes" className="mt-2 w-full rounded-2xl border border-white/10 bg-black px-4 py-3 text-white"/></label></div><div className="mt-5 rounded-3xl border border-white/10 bg-black/35 p-4"><p className="text-xs font-black uppercase tracking-widest text-white/40">Message to copy</p><p className="mt-3 whitespace-pre-wrap text-sm text-white/75">{template?.message_body||"Select a template"}</p><button type="button" onClick={()=>navigator.clipboard.writeText(template?.message_body||"")} className="mt-3 rounded-full bg-white px-4 py-2 text-xs font-black text-black">Copy message</button></div><button className="mt-5 rounded-full bg-gradient-to-r from-rose-500 to-rose-700 px-5 py-3 text-sm font-black text-white">Submit for review</button>{msg?<p className="mt-3 text-sm font-bold text-white/65">{msg}</p>:null}</form> }
