import { describe, expect, it } from "vitest";
import { activityRetrievalTerms, canonicalTaxonomy, validateCanonicalTaxonomy } from ".";
describe("canonical taxonomy", () => { it("is internally valid", () => expect(validateCanonicalTaxonomy()).toEqual([])); it("derives generic activity retrieval from canonical entries", () => { expect(activityRetrievalTerms("bowling")).toContain("bowling"); expect(canonicalTaxonomy.some((item) => item.id === "live_music")).toBe(true); expect(activityRetrievalTerms("live_music")).toContain("jazz"); expect(canonicalTaxonomy.some((item) => item.id === "party_venue")).toBe(true); expect(activityRetrievalTerms("party_venue")).toContain("party venue"); expect(activityRetrievalTerms("party_venue")).not.toContain("event venue"); }); });
