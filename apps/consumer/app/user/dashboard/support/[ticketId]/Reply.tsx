"use client";import{useState}from"react";export default function Reply({ticketId}:{ticketId:string}){const[m,setM]=useState("");const[s,setS]=useState("");return <form onSubmit={async e=>{e.preventDefault();const r=await fetch(`/api/user/support/tickets/${ticketId}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:m})});setS(r.ok?'Reply sent. Refresh to see it.':'Could not send reply.');setM('')}} className="mt-6 grid gap-3"><textarea className="min-h-28 rounded-2xl bg-black/30 p-3" value={m} onChange={e=>setM(e.target.value)} placeholder="Reply"/><button className="rounded-full bg-rose-600 px-5 py-3 font-black">Send reply</button>{s&&<p className="text-sm text-rose-100">{s}</p>}</form>}
