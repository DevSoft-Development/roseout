import { requireAdminRole } from "@/lib/admin-auth"; import { CRM_READ_ROLES } from "@/lib/crm/permissions"; import { CommunicationsFrame } from "../CommunicationTabs"; export const dynamic="force-dynamic"; export default async function Page(){await requireAdminRole(CRM_READ_ROLES);return <CommunicationsFrame active="contacts" title="Contacts" description="Canonical contacts communication controls and records."><section className="rounded-2xl border border-white/10 bg-[#111] p-6"><h2 className="text-lg font-black">Contacts</h2><p className="mt-2 text-sm text-white/55">Use the shared inbox and controlled compose pipeline. Consent, suppression, approval, and sender identity are rechecked server-side before delivery.</p></section></CommunicationsFrame>}
