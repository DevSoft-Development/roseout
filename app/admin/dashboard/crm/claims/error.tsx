"use client";
import Link from "next/link";
export default function ClaimsError({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) { const ref = error.digest || `claims-${Date.now().toString(36)}`; return <main className="m-6 rounded-3xl border border-rose-300/30 bg-rose-950/30 p-6 text-white"><p className="text-xs font-black uppercase tracking-[.25em] text-rose-200">Claims unavailable</p><h1 className="mt-2 text-3xl font-black">Claims could not be loaded safely.</h1><p className="mt-2 text-white/70">The CRM received a structured claims-query failure. Raw database details are hidden; share diagnostic reference <b>{ref}</b> with engineering.</p><div className="mt-5 flex flex-wrap gap-3"><button onClick={reset} className="rounded-xl bg-white px-4 py-2 font-black text-black">Retry</button><Link href="/admin/dashboard/crm" className="rounded-xl border border-white/15 px-4 py-2 font-bold">Back to CRM</Link></div></main> }
