import { FoundationScreen } from "@/components/FoundationScreen";

export default function ExploreScreen() {
  return (
    <FoundationScreen
      eyebrow="EXPLORE"
      title="Discover what is around you"
      description="Explore will surface occasions, neighborhoods, restaurants, activities, and curated OUTing ideas without duplicating the search flow."
    />
  );
}
