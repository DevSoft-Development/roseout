import ReserveStatusBadge from "./ReserveStatusBadge";
export default function ReservationSidePanel({ reservation }: { reservation?: { customer_name?: string | null; status?: string | null; special_request?: string | null } | null }) { if (!reservation) return <div className="rounded-2xl border border-white/10 bg-white/[0.05] p-5 text-sm text-white/55">Select a reservation to see guest details and safe next steps.</div>; return <aside className="rounded-2xl border border-white/10 bg-white/[0.05] p-5"><ReserveStatusBadge status={reservation.status}/><h3 className="mt-4 text-xl font-black">{reservation.customer_name || "Guest"}</h3><p className="mt-2 text-sm text-white/55">{reservation.special_request || "No guest notes yet."}</p></aside>; }
