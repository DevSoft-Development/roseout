import { FoundationScreen } from "@/components/FoundationScreen";

export default function OutingsScreen() {
  return (
    <FoundationScreen
      eyebrow="OUTINGS"
      title="Your OUTings"
      description="Upcoming, saved, active, completed, and favorite outing states will live here as the consumer lifecycle hub."
    />
  );
}
