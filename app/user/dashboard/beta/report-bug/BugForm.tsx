"use client";import {useState} from "react";export default function BugForm({defaults}:{defaults:any}){const[m,setM]=useState('');async function sub(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch('/api/beta/bug-report',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...Object.fromEntries(fd),assignmentId:defaults.assignmentId,page_url:defaults.route||location.href})});setM(r.ok?'Bug report submitted.':'Could not submit bug report.')}return <form onSubmit={sub} className="mt-5 grid gap-3"><input name="title" required placeholder="Bug type/category" className="rounded-2xl bg-black/30 p-3"/><textarea name="description" required placeholder="What happened?" className="rounded-2xl bg-black/30 p-3"/><textarea name="expected_result" placeholder="What did you expect?" className="rounded-2xl bg-black/30 p-3"/><textarea name="steps_to_reproduce" placeholder="Steps to reproduce" className="rounded-2xl bg-black/30 p-3"/><select name="severity" className="rounded-2xl bg-black/30 p-3"><option>low</option><option>medium</option><option>high</option></select><button className="rounded-full bg-rose-600 px-5 py-3 font-black">Submit Bug Report</button>{m&&<p>{m}</p>}</form>}
