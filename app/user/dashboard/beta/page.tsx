import Link from "next/link";import UserDashboardShell,{DashboardCard} from "@/components/user/UserDashboardShell";import { getCurrentUserDashboardContext } from "@/lib/user-dashboard";import { assignWeeklyBetaTasksForTester,getCurrentWeekStart } from "@/lib/beta/weeklyTasks";import { supabaseAdmin } from "@/lib/supabaseAdmin";import BetaDashboardClient from "@/app/beta/dashboard/BetaDashboardClient";export const dynamic="force-dynamic";export default async function Page(){const ctx=await getCurrentUserDashboardContext();if(!ctx.isBeta)return <UserDashboardShell><DashboardCard><h1 className="text-3xl font-black">Beta access required</h1><p className="mt-2 text-white/60">This area is for active beta testers.</p><Link href="/user/dashboard" className="mt-5 inline-flex rounded-full bg-rose-600 px-5 py-3 text-sm font-black">Back to dashboard</Link></DashboardCard></UserDashboardShell>;await assignWeeklyBetaTasksForTester(ctx.beta.id);const week=getCurrentWeekStart();const {data:assignments}=await supabaseAdmin.from('beta_task_assignments').select('*, beta_tasks(*)').eq('tester_id',ctx.beta.id).eq('assigned_week_start',week).order('created_at',{ascending:true});const completed=(assignments||[]).filter((a:any)=>a.status==='completed').length;return <UserDashboardShell isBeta><section className="rounded-[2rem] border border-white/10 bg-[radial-gradient(circle_at_top_left,rgba(225,29,72,.18),transparent_30%),#120d0b] p-6"><p className="text-xs font-black uppercase tracking-[.32em] text-rose-200">TheOutHaven Beta Member</p><h1 className="mt-3 text-4xl font-black">Your weekly beta goal: 5 quick tests</h1><p className="mt-3 text-white/70">Try searches, review results, then tell us what worked and what felt off.</p><div className="mt-5 h-3 rounded-full bg-white/10"><div className="h-3 rounded-full bg-rose-600" style={{width:`${Math.min(100,completed*20)}%`}}/></div><p className="mt-2 text-sm text-white/65">Progress: {completed}/5 completed</p></section><div className="mt-4 flex gap-2"><Link href="/user/dashboard/beta/feedback" className="rounded-full border border-white/10 px-4 py-2 text-sm font-bold">Submit Feedback</Link><Link href="/user/dashboard/beta/report-bug" className="rounded-full border border-white/10 px-4 py-2 text-sm font-bold">Report Bug</Link></div>{assignments?.length?<div className="mt-6"><BetaDashboardClient assignments={assignments}/></div>:<DashboardCard className="mt-6"><h2 className="text-2xl font-black">No active tasks</h2><p className="mt-2 text-white/60">Feedback and bug reports are still open.</p></DashboardCard>}</UserDashboardShell>}
