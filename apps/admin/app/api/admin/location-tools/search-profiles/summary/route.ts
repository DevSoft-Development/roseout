import { requireAdminApiRole } from "@/lib/admin-api-auth";
import { supabaseAdmin } from "@/lib/supabase-admin";
import { NextResponse } from "next/server";
export async function GET(){const auth=await requireAdminApiRole(["superadmin","admin"]);if(auth.error)return auth.error;const [eligible,profiles,stale,review,low,failed,active]=await Promise.all([supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).eq("active",true).eq("searchable",true).eq("hidden",false),supabaseAdmin.from("location_search_profiles").select("location_id",{count:"exact",head:true}),supabaseAdmin.from("location_search_profiles").select("location_id",{count:"exact",head:true}).lt("profile_version",3),supabaseAdmin.from("location_search_profiles").select("location_id",{count:"exact",head:true}).eq("needs_review",true),supabaseAdmin.from("location_search_profiles").select("location_id",{count:"exact",head:true}).lt("confidence",.55),supabaseAdmin.from("location_search_profile_refresh_queue").select("id",{count:"exact",head:true}).eq("status","failed"),supabaseAdmin.from("location_search_profile_runs").select("id",{count:"exact",head:true}).in("status",["pending","running","cancelling"])]);const all=[eligible,profiles,stale,review,low,failed,active];const error=all.find((item)=>item.error)?.error;if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({eligibleLocations:eligible.count??0,profilesGenerated:profiles.count??0,missingProfiles:Math.max(0,(eligible.count??0)-(profiles.count??0)),staleProfiles:stale.count??0,needsReview:review.count??0,lowConfidence:low.count??0,failedQueueItems:failed.count??0,activeBackfillRuns:active.count??0});}
