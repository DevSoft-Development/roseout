import { requireAdminRole } from "@/lib/admin-auth";
import { supabaseAdmin } from "@/lib/supabase-admin";
import { LocationToolShell, ToolCard } from "@/components/admin/location-tools/LocationToolShell";
import { GoogleEnrichmentClient } from "@/components/admin/location-tools/GoogleEnrichmentClient";
import { ActionToolsClient } from "@/components/admin/location-tools/ActionToolsClient";
export const dynamic = "force-dynamic";
export default async function Page(){ await requireAdminRole(["superadmin","admin"]); const {data}= await supabaseAdmin.from("location_google_food_term_suggestions").select("*").order("created_at",{ascending:false}).limit(100); return <LocationToolShell title="Enrichment" description="Review Google Places suggestions and run small category correction batches for high-value imported locations." stats={[{label:"Loaded suggestions", value:data?.length||0, tone:"rose"}]}><ToolCard title="Google Places enrichment review" description="Preview enrichment, create review suggestions, and approve or reject proposed search terms."><GoogleEnrichmentClient initialSuggestions={(data||[]) as any}/></ToolCard><ToolCard title="Category correction / high-value enrichment" description="Runs the existing high-value enrichment route for a bounded maintenance pass."><ActionToolsClient actions={[{label:"Run high-value enrichment", endpoint:"/api/admin/location-growth/enrich-high-value", body:{limit:25}, tone:"emerald"}]} /></ToolCard></LocationToolShell> }
