import { supabaseAdmin } from "@/lib/supabase-admin";
import { sendGrowthProEmail } from "./email";
export async function createLocationNotificationEvent(input:{locationId:string;eventType:string;title:string;message:string;priority?:string;metadata?:Record<string,any>;businessEmail?:string;templateKey?:string}){ let id:string|undefined; try{ const {data}=await supabaseAdmin.from("location_notification_events").insert({location_id:input.locationId,event_type:input.eventType,title:input.title,message:input.message,priority:input.priority||"normal",metadata:input.metadata||{},read_at:null,archived_at:null}).select("id").single(); id=data?.id; }catch(e){console.warn("notification insert skipped",e);} if(input.businessEmail&&input.templateKey&&(input.priority==="high"||input.priority==="urgent")) await sendGrowthProEmail(input.businessEmail,input.templateKey,{locationId:input.locationId,title:input.title,message:input.message,...input.metadata}); return {id}; }
