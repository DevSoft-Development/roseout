import { requireAdminRole } from "@/lib/admin-auth";
import { supabaseAdmin } from "@/lib/supabase-admin";
import { LocationToolShell, ToolCard } from "@/components/admin/location-tools/LocationToolShell";
import { ActionToolsClient } from "@/components/admin/location-tools/ActionToolsClient";
export const dynamic="force-dynamic";
export default async function Page(){ await requireAdminRole(["superadmin","admin"]); const [searchable, notSearchable, ready, review] = await Promise.all([supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).eq("is_searchable",true), supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).or("is_searchable.eq.false,is_searchable.is.null"), supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).eq("data_status","ready_to_approve"), supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).eq("data_status","needs_review")]); return <LocationToolShell title="Publishing" description="Repair publishability and publish bounded staged batches that are ready for public search." stats={[{label:"Searchable",value:searchable.count||0,tone:"emerald"},{label:"Not searchable",value:notSearchable.count||0,tone:"amber"},{label:"Ready to approve",value:ready.count||0,tone:"rose"},{label:"Needs review",value:review.count||0,tone:"white"}]}><ToolCard title="Publishing and searchability actions" description="Publishing/searchability changes affect public search results."><ActionToolsClient warning="Publishing/searchability changes affect public search results." inputLabel="Single location ID" actions={[{label:"Publish staged ready batch",endpoint:"/api/admin/location-growth/publish",body:{limit:25},tone:"rose"},{label:"Dry-run batch repair",endpoint:"/api/admin/locations/repair-publishability",body:{action:"repair",dryRun:true,limit:50},tone:"white"},{label:"Apply batch repair",endpoint:"/api/admin/locations/repair-publishability",body:{action:"repair",dryRun:false,limit:50},tone:"emerald"},{label:"Repair one location",endpoint:"/api/admin/locations/repair-publishability",body:{action:"repair",dryRun:false},requireInput:"locationId",tone:"rose"}]} /></ToolCard></LocationToolShell> }
