# Mobile discovery

The Home discovery surface is intentionally lightweight. It gathers a user intent and hands it to the native Plan route through the `prompt` route parameter.

The search engine remains server-owned. Home does not parse, rank, pair, or enrich locations.

PR 6 should consume the existing `prompt` parameter when implementing the full PLAN → MAKE IT YOURS → PICK → COMPLETE OUTING flow.
