import Link from 'next/link';
import CrmWorkspaceShell from '@/components/admin/crm/CrmWorkspaceShell';
import GtmPriorityPanel from '@/components/admin/crm/GtmPriorityPanel';
import { requireAdminRole } from '@/lib/admin-auth';
import { CRM_READ_ROLES } from '@/lib/crm/permissions';
import { getGtmCommandCenter,getGtmPriorityQueue } from '@/lib/gtm/queries';
import { getGtmRevenueIntelligence } from '@/lib/gtm/revenue';
export const dynamic='force-dynamic';
export default async function GtmPage(){await requireAdminRole(CRM_READ_ROLES); const [metrics,priority,revenue]=await Promise.all([getGtmCommandCenter(),getGtmPriorityQueue(50),getGtmRevenueIntelligence()]); const cards=[['Observed',metrics.observed],['Full CRM',metrics.full],['Hot',metrics.hot],['Warm',metrics.warm],['Engaged',metrics.engaged],['Customers',metrics.customers],['MRR',`$${revenue.mrr.toLocaleString()}`],['Avg demand',metrics.avgDemand]]; return <CrmWorkspaceShell><main className="space-y-5 text-white"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-rose-300">TheOutHaven GTM</p><h1 className="mt-1 text-3xl font-black">Revenue Intelligence</h1><p className="mt-1 max-w-3xl text-white/55">Searchable businesses → qualification → contact discovery → outreach → engagement → claim → activation → paid → expansion.</p></div><div className="flex flex-wrap gap-2"><Link href="/admin/dashboard/crm/today" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-black">Today</Link><Link href="/admin/dashboard/crm/gtm/sequences" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-black">Claim Sequences</Link><Link href="/admin/dashboard/crm/gtm/territories" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-black">Territories</Link><Link href="/admin/dashboard/crm/gtm/attribution" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-black">Attribution & Heat</Link><Link href="/admin/dashboard/crm/gtm/revenue" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-black">Revenue</Link></div></header><section className="grid grid-cols-2 gap-3 lg:grid-cols-4">{cards.map(([label,value])=><div key={String(label)} className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><b className="text-2xl text-white">{value}</b><small className="mt-1 block font-bold text-white/50">{label}</small></div>)}</section><GtmPriorityPanel rows={priority}/><section className="grid gap-4 lg:grid-cols-3"><div className="rounded-3xl border border-white/10 bg-[#0e0e11] p-5"><h2 className="font-black">Scoring</h2><p className="mt-2 text-sm text-zinc-500">Opportunity = gaps 30 + quality 20 + demand 20 + engagement 15 + territory 10 + contactability 5. Demand, contactability and activation remain separate scores.</p></div><div className="rounded-3xl border border-white/10 bg-[#0e0e11] p-5"><h2 className="font-black">Association</h2><p className="mt-2 text-sm text-zinc-500">Every searchable business gets lightweight GTM state. Full CRM activates at 60+, meaningful intent, claim activity, manual qualification, or customer status. Contact discovery improves contactability but does not activate a weak business by itself.</p></div><div className="rounded-3xl border border-white/10 bg-[#0e0e11] p-5"><h2 className="font-black">Next Best Action</h2><p className="mt-2 text-sm text-zinc-500">Suppression first, then activation/expansion, claim follow-up, engaged follow-up, email, call, postcard, research or monitor.</p></div></section></main></CrmWorkspaceShell>}
