import Link from 'next/link';
import CrmWorkspaceShell from '@/components/admin/crm/CrmWorkspaceShell';
import { requireAdminRole } from '@/lib/admin-auth';
import { CRM_READ_ROLES } from '@/lib/crm/permissions';
import { getGtmAttributionSummary } from '@/lib/gtm/attribution';
import { getGtmHeatSignals } from '@/lib/gtm/benchmarks';
export const dynamic='force-dynamic';
export default async function Page(){await requireAdminRole(CRM_READ_ROLES);const [a,heat]=await Promise.all([getGtmAttributionSummary(),getGtmHeatSignals(20)]);return <CrmWorkspaceShell><main className="space-y-5 text-white"><header className="flex items-end justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.2em] text-rose-300">GTM Attribution</p><h1 className="mt-1 text-3xl font-black">Channels, Creators & Market Heat</h1><p className="mt-1 text-white/55">First/last/assisted GTM signals, creator/referral performance and high-demand low-coverage areas.</p></div><Link href="/admin/dashboard/crm/gtm" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-black">GTM</Link></header><section className="grid gap-5 xl:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-[#0e0e11] p-5"><h2 className="text-xl font-black">Channel activity</h2><div className="mt-3 divide-y divide-white/10">{a.channels.map((x:any)=><div key={x.channel} className="flex justify-between py-3 text-sm"><b>{x.channel}</b><span className="text-zinc-400">{x.events} events · {x.claims} claims</span></div>)}</div></div><div className="rounded-3xl border border-white/10 bg-[#0e0e11] p-5"><h2 className="text-xl font-black">Creator performance</h2>{a.creators.length?<div className="mt-3 divide-y divide-white/10">{a.creators.map((x:any)=><div key={x.creator_key} className="flex justify-between py-3 text-sm"><b>{x.display_name}</b><span className="text-zinc-400">{x.events} events · {x.claims} claims · {x.paid} paid</span></div>)}</div>:<p className="mt-3 text-sm text-zinc-500">No creator sources recorded yet.</p>}</div></section><section className="rounded-3xl border border-white/10 bg-[#0e0e11] p-5"><h2 className="text-xl font-black">Market heat</h2><p className="mt-1 text-sm text-zinc-500">High demand with low customer penetration rises to the top.</p><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="text-zinc-500"><tr><th>Area</th><th>Category</th><th>Businesses</th><th>Avg demand</th><th>Customer coverage</th><th>Heat</th></tr></thead><tbody>{heat.map((x:any)=><tr key={`${x.area}-${x.category}`} className="border-t border-white/10"><td className="py-3 font-black">{x.area}</td><td>{x.category}</td><td>{x.businesses}</td><td>{x.avgDemand}</td><td>{x.coveragePct}%</td><td className="font-black text-rose-200">{x.heatScore}</td></tr>)}</tbody></table></div></section></main></CrmWorkspaceShell>}
