import { notFound,redirect } from "next/navigation";import { requireAdminRole } from "@theouthaven/auth/admin-session";import { getAdminDatabaseClient } from "@theouthaven/db/admin-client";import { getKbCategories,KB_SELECT } from "@/lib/knowledge-base/server";import { roleCanEditKb } from "@/lib/knowledge-base/access";import ArticleForm from "../../ArticleForm";import KbTabs from "../../KbTabs";export default async function Page({params}:{params:Promise<{slug:string}>}){const admin=await requireAdminRole(["superadmin","admin","editor"]);if(!roleCanEditKb(admin.role))redirect("/admin/unauthorized");const{slug}=await params;const[{data},categories]=await Promise.all([getAdminDatabaseClient().from("knowledge_base_articles").select(KB_SELECT).eq("slug",slug).maybeSingle(),getKbCategories()]);if(!data)notFound();return <main className="min-h-screen bg-[#08050b] p-6 text-white"><div className="mx-auto max-w-5xl space-y-6"><h1 className="text-4xl font-black">Edit Knowledge Base Article</h1><KbTabs/><ArticleForm article={data} categories={categories}/></div></main>}
