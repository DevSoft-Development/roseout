"use client";import { useState } from "react";export default function AiClient(){const[q,setQ]=useState("");const[answer,setAnswer]=useState("");const[busy,setBusy]=useState(false);async function ask(){setBusy(true);const res=await fetch("/api/admin/knowledge-base/ai",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({question:q})});const data=await res.json();setAnswer(data.answer||data.error||"No answer");setBusy(false)}return <div className="space-y-4"><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask the approved knowledge base..." className="min-h-32 w-full rounded-2xl bg-black/40 p-4"/><button disabled={busy||q.trim().length<3} onClick={ask} className="rounded-xl bg-rose-600 px-5 py-3 font-black disabled:opacity-40">{busy?"Searching...":"Ask Knowledge Base"}</button>{answer&&<div className="whitespace-pre-wrap rounded-3xl border border-white/10 bg-white/[0.04] p-6 leading-7">{answer}</div>}</div>}
