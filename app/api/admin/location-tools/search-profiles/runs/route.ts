import { requireAdminApiRole } from "@/lib/admin-api-auth";import { createProfileRun } from "@/lib/search/profile/profileRunRepository";import { supabaseAdmin } from "@/lib/supabase-admin";import { NextResponse } from "next/server";
export async function GET(){const auth=await requireAdminApiRole(["superadmin","admin"]);if(auth.error)return auth.error;const result=await supabaseAdmin.from("location_search_profile_runs").select("*").order("created_at",{ascending:false}).limit(100);return result.error?NextResponse.json({error:result.error.message},{status:500}):NextResponse.json({runs:result.data});}
export async function POST(request:Request){const auth=await requireAdminApiRole(["superadmin","admin"]);if(auth.error)return auth.error;const body=await request.json();if(typeof body.mode!=="string")return NextResponse.json({error:"mode is required"},{status:400});const ids=Array.isArray(body.locationIds)&&body.locationIds.every((id:unknown)=>typeof id==="string")?body.locationIds:undefined;try{return NextResponse.json({run:await createProfileRun({mode:body.mode,filters:typeof body.filters==="object"&&body.filters?body.filters:{},configuration:typeof body.configuration==="object"&&body.configuration?body.configuration:{},requestedBy:auth.adminUser?.user_id??"",locationIds:ids})},{status:201});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Run creation failed"},{status:500});}}
