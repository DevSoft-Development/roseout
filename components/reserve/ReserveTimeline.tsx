"use client";
import { formatReservationTime, getReservationPrimaryNextAction, getReservationStatusLabel } from '@/lib/reservations/ui';import ReserveQuickActionButton from './ReserveQuickActionButton';import ReserveStatusBadge from './ReserveStatusBadge';
function assigned(r:any){return r.assigned_resource_label||r.reservable_item_name||r.resource_label||'Unassigned'}
export default function ReserveTimeline({reservations,selectedId,onSelect,onStatus,onAssign,updatingId}:{reservations:any[];selectedId?:string;onSelect:(r:any)=>void;onStatus:(r:any,s:string)=>void;onAssign?:(r:any)=>void;updatingId?:string}){return <div className="space-y-2">{reservations.map(r=>{const action=getReservationPrimaryNextAction(r.status);return <button type="button" key={r.id} onClick={()=>onSelect(r)} className={`reserve-soft w-full rounded-2xl px-3 py-2.5 text-left transition hover:scale-[1.005] ${selectedId===r.id?'ring-2 ring-red-600 bg-red-500/10':''}`}><div className="grid gap-2 sm:grid-cols-[72px_1fr_auto] sm:items-center"><div><p className="text-sm font-black">{formatReservationTime(r.reservation_time)}</p></div><div className="min-w-0"><div className="flex flex-wrap items-center gap-1.5"><h3 className="truncate text-sm font-black md:text-base">{r.customer_name||'Guest'}</h3><ReserveStatusBadge status={r.status} label={getReservationStatusLabel(r.status)}/></div><p className="mt-0.5 truncate text-xs reserve-muted">Party {r.party_size||'—'} · {assigned(r)}</p></div><div className="flex flex-wrap gap-1.5 sm:justify-end" onClick={(e)=>e.stopPropagation()}><ReserveQuickActionButton disabled={updatingId===r.id || !!action.disabledReason || !action.targetStatus} title={action.disabledReason} onClick={()=>action.targetStatus&&onStatus(r,action.targetStatus)}>{updatingId===r.id?'Updating…':action.label}</ReserveQuickActionButton>{onAssign&&<ReserveQuickActionButton onClick={()=>onAssign(r)}>Assign table</ReserveQuickActionButton>}</div></div></button>})}</div>}
