import { NextResponse } from 'next/server'; import { supabaseAdmin } from '@/lib/supabase-admin'; import { requireAdminApiRole } from '@/lib/admin-api-auth';
import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions";
export async function POST(){const a=await requireAdminApiRole(ADMIN_PAGE_ACCESS.seoEdit); if(a.error)return a.error; const started=new Date().toISOString(); const run=(await supabaseAdmin.from('seo_audit_runs').insert({run_type:'audit',status:'completed',started_at:started,completed_at:new Date().toISOString(),pages_scanned:0,issues_found:0,warning_count:0,critical_count:0,improvement_count:0,passed_count:0,metadata:{warning:'Filesystem checks skipped in runtime environment'}}).select('*').single()).data; return NextResponse.json({run,warning:'Filesystem checks may be unavailable in this environment.'});}
