name: Recover daily emails after Integration API mail deploy

on:
  workflow_run:
    workflows: ['AWS Integration API SES fallback']
    types: [completed]

permissions:
  actions: write
  contents: read

jobs:
  recover:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch retained daily email recovery
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh workflow run aws-daily-email-aws-recovery.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref main
          echo 'retained_daily_email_recovery=dispatched'
