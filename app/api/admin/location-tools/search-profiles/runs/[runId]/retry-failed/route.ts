import { requireAdminApiRole } from "@/lib/admin-api-auth";import { resumeProfileRun } from "@/lib/search/profile/profileRunRepository";import { NextResponse } from "next/server";export async function POST(_r:Request,{params}:{params:Promise<{runId:string}>}){const auth=await requireAdminApiRole(["superadmin","admin"]);if(auth.error)return auth.error;try{return NextResponse.json({run:await resumeProfileRun((await params).runId,true)});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Retry failed"},{status:500});}}
