import { requireAdminRole } from "@/lib/admin-auth";
import { supabaseAdmin } from "@/lib/supabase-admin";
import { LocationToolShell, ToolCard } from "@/components/admin/location-tools/LocationToolShell";
import { DuplicateReviewClient } from "@/components/admin/location-tools/DuplicateReviewClient";
export const dynamic="force-dynamic";
export default async function Page(){ await requireAdminRole(["superadmin","admin"]); const [possible, decided] = await Promise.all([supabaseAdmin.from("location_import_staging").select("id",{count:"exact",head:true}).eq("duplicate_status","possible_duplicate"), supabaseAdmin.from("location_duplicate_matches").select("id",{count:"exact",head:true}).not("decision","is",null)]); return <LocationToolShell title="Duplicates" description="Review staged duplicate matches with bounded scans so page loads remain timeout-safe." stats={[{label:"Possible duplicates",value:possible.count||0,tone:"amber"},{label:"Decisions recorded",value:decided.count||0,tone:"emerald"}]}><ToolCard title="Duplicate scan and review" description="Loads a small batch from the existing duplicate match APIs and lets admins record supported decisions."><DuplicateReviewClient /></ToolCard></LocationToolShell> }
