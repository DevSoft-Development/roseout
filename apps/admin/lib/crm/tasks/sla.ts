export type SlaStatus="on_track"|"due_soon"|"at_risk"|"breached"|"not_applicable";
export function addBusinessHours(start:Date,hours:number){const d=new Date(start);let remaining=hours;while(remaining>0){d.setHours(d.getHours()+1);const day=d.getDay(),hour=d.getHours();if(day!==0&&day!==6&&hour>9&&hour<=17)remaining--}return d}
export function slaHours(task:{escalation_level?:string;priority?:string;queue_key?:string}){if(task.escalation_level==="critical")return 1;if(task.priority==="urgent"&&["support","reservations"].includes(task.queue_key||""))return 4;if(task.priority==="high"&&["claims","billing"].includes(task.queue_key||""))return 8;if(task.queue_key==="outreach"||task.queue_key==="sales")return 16;return 40}
export function calculateSlaStatus(due:string|null|undefined,now=new Date()):SlaStatus{if(!due)return "not_applicable";const delta=new Date(due).getTime()-now.getTime();if(delta<0)return "breached";if(delta<=60*60*1000)return "at_risk";if(delta<=4*60*60*1000)return "due_soon";return "on_track"}
