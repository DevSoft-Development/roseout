import { NextRequest, NextResponse } from "next/server";
import { runEnterpriseSearch } from "@/lib/search/enterprise";
import { getSearchSpeedStatus } from "@/lib/search/performance";
import { requireBetaAdmin, safeError } from "../_shared";
export async function POST(req:NextRequest){const a=await requireBetaAdmin(); if(a.error)return a.error; try{const b=await req.json(); const query=String(b.query||"").trim(); if(!query)return safeError("query required",400); const result=await runEnterpriseSearch(query,{useLLM:true,source:"admin_search_lab",route:"/api/admin/beta/search-lab",logPerformance:true,betaDebug:true,betaAssignmentId:b.betaAssignmentId??null,betaTesterId:b.betaTesterId??null,usedCustomPrompt:!!b.usedCustomPrompt}); const perf=(result.debug as any)?.performance||{}; return NextResponse.json({success:true,reply:result.reply,restaurants:result.restaurants?.length||0,activities:result.activities?.length||0,pairs:result.pairs?.length||0,cards:[...(result.restaurants||[]),...(result.activities||[])],parsedIntent:(result.debug as any)?.normalizedIntent,performance:perf,rejectedReasons:{restaurants:(result.debug as any)?.restaurantRejectedSummary,activities:(result.debug as any)?.activityRejectedSummary},speedStatus:getSearchSpeedStatus({totalMs:perf.total_ms,success:true}),missingPhotoResultsRemoved:true,fallbackUsed:Boolean((result.debug as any)?.restaurantRecoveryUsed||(result.debug as any)?.activityRecoveryUsed),customPrompt:!!b.usedCustomPrompt,debug:result.debug});}catch(e){console.error(e);return safeError("Search Lab failed");}}
