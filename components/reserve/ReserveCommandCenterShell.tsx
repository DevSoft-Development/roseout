"use client";
import { ReactNode, useEffect, useState } from "react";
import ReserveThemeToggle, { getInitialReserveTheme, type ReserveTheme } from "./ReserveThemeToggle";
import ReserveSidebar from "./ReserveSidebar";
import ReserveTopBar from "./ReserveTopBar";
export default function ReserveCommandCenterShell(props:{children:ReactNode; locationName?:string; locationId?:string; locationType?:string; activeTab:string; activeSection?:string; onTabChange:(tab:string)=>void; actions?:ReactNode; setupEnabled?:boolean; userLabel?:string; actingContext?: { adminLocationId?: string; type?: string }}){ const [theme,setTheme]=useState<ReserveTheme>('dark'); useEffect(()=>setTheme(getInitialReserveTheme()),[]); function change(next:ReserveTheme){ setTheme(next); localStorage.setItem('theouthaven_reserve_theme', next); } return <main className={`reserve-command-center reserve-theme-${theme} h-dvh overflow-hidden`}><div className="grid h-dvh min-h-0 lg:grid-cols-[260px_minmax(0,1fr)]"><ReserveSidebar {...props} actingContext={props.actingContext}/><section className="min-w-0 overflow-y-auto overflow-x-hidden bg-[var(--reserve-bg)] px-3 pb-5 sm:px-5 lg:px-6"><ReserveTopBar {...props} themeToggle={<ReserveThemeToggle theme={theme} onChange={change}/>} />{props.children}</section></div></main> }
