import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase-server";
export async function PATCH(req:Request){const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user)return NextResponse.json({success:false,error:"Unauthorized"},{status:401}); const b=await req.json().catch(()=>({})); const safe:any={}; ["full_name","preferred_name","zip_code","mobile_number","phone","age_range","birthday_month","birthday_day","birthday_opt_in","sms_opt_in"].forEach(k=>{if(k in b)safe[k]=b[k]}); if("preferences" in b)safe.preferences=b.preferences||{}; safe.updated_at=new Date().toISOString(); const {data,error}=await supabase.from("user_profiles").upsert({id:user.id,email:user.email,...safe},{onConflict:"id"}).select("*").single(); if(error)return NextResponse.json({success:false,error:error.message},{status:400}); return NextResponse.json({success:true,profile:data});}
