const defaultItems=["Business info","Hours","Layout/capacity","Owner contact","Reminder settings","Test booking","Publish booking link"];
export default function ReserveSetupChecklist({ completed = [] }: { completed?: string[] }) { return <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{defaultItems.map(item => { const done=completed.includes(item); return <div key={item} className={`rounded-2xl border p-4 ${done ? "border-emerald-300/25 bg-emerald-500/10" : "border-white/10 bg-white/[0.05]"}`}><p className="text-sm font-black">{done ? "✓ " : "○ "}{item}</p><p className="mt-1 text-xs text-white/45">{done ? "Ready" : "Needs setup"}</p></div>})}</div>; }
