import Link from "next/link"; import TeamDemoClient from "@/components/TeamDemoClient"; import { ensureTeamProfileForCurrentUser } from "@/lib/team-tools"; import { supabaseAdmin } from "@/lib/supabase-admin";
export const dynamic="force-dynamic";
export default async function TeamDemo(){ const {user,profile}=await ensureTeamProfileForCurrentUser(); if(!profile.can_use_demo_mode) return <main className="min-h-screen bg-[#080808] p-6 text-white"><Link href="/team">← Team</Link><h1 className="mt-6 text-3xl font-black">Demo mode is not enabled for your profile.</h1></main>; const [{data:masters=[]},{data:sessions=[]}]=await Promise.all([supabaseAdmin.from("crm_demo_locations").select("*").eq("is_active",true).order("demo_name"), supabaseAdmin.from("crm_demo_sessions").select("*").eq("user_id",user.id).order("created_at",{ascending:false})]); return <main className="min-h-screen bg-[#080808] px-4 py-8 text-white"><div className="mx-auto max-w-6xl"><Link href="/team" className="text-sm font-black text-rose-200">← Team</Link><h1 className="mt-6 text-3xl font-black">Training Mode</h1><div className="mt-6 rounded-3xl border border-amber-400/25 bg-amber-500/10 p-4 text-sm font-black text-amber-100">DEMO MODE — Training only. No real booking will be created.</div><div className="mt-6"><TeamDemoClient masters={masters || []} sessions={sessions || []}/></div></div></main>}
