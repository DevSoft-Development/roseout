import Link from "next/link";
import { requireAdminRole } from "@/lib/admin-auth";
import { listAssignableTeamMembers, searchWorkspaceLocationsForUser } from "@/lib/team-tools";
import AdminAssignLocationsClient from "@/components/AdminAssignLocationsClient";
export const dynamic="force-dynamic";
export default async function AssignLocationsPage({searchParams}:{searchParams:Promise<{q?:string}>}){const admin=await requireAdminRole(["superadmin","admin"]); const sp=await searchParams; const [locations,teamMembers]=await Promise.all([searchWorkspaceLocationsForUser(admin.user_id, admin.role, sp.q||"", {limit:50}), listAssignableTeamMembers()]); return <main className="px-4 pb-12 pt-6 text-white sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl space-y-6"><Link href="/admin/dashboard/my-workspace" className="text-sm font-black text-rose-200">← Admin Workspace</Link><section className="rounded-3xl border border-white/10 bg-[radial-gradient(circle_at_top_left,rgba(225,29,72,.22),transparent_30%),#120d0b] p-6"><p className="text-xs font-black uppercase tracking-[.3em] text-rose-200">Partner Launch workspace</p><h1 className="mt-3 text-4xl font-black">Assign Locations</h1><p className="mt-3 max-w-3xl text-sm font-bold leading-6 text-white/60">Add locations to a workspace queue by location, neighborhood, category, claim status, or CRM status.</p></section><AdminAssignLocationsClient initialLocations={locations} teamMembers={teamMembers}/></div></main>}
