import { NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/supabase-admin";
import { requireTurnstile } from "@/lib/security/turnstile";
import { trackGrowthProEvent } from "@/lib/growth-pro/analytics";
export async function POST(request: Request){const body=await request.json().catch(()=>({})); const check=await requireTurnstile({request,token:body.turnstileToken,action:"event_lead"}); if(!check.success)return NextResponse.json({error:check.error},{status:check.status}); const locationId=String(body.locationId||""); if(!locationId)return NextResponse.json({error:"Please choose a location before submitting."},{status:400}); await supabaseAdmin.from("location_leads").insert({location_id:locationId,lead_type:body.leadType||"private_event",customer_name:body.name,customer_email:body.email,customer_phone:body.phone,occasion:body.occasion,guest_count:body.guestCount,notes:body.notes,source:"public_growth_pro"}); await supabaseAdmin.from("location_notification_events").insert({location_id:locationId,event_type:"private_event_lead_created",title:"New event lead",message:"A customer requested private event or group package information.",priority:"high"}).then(undefined,()=>undefined); await trackGrowthProEvent(locationId,"event_lead_submitted",{}); return NextResponse.json({message:"Thanks — your event request was sent."});}
