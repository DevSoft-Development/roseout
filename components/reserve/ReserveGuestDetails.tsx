"use client";
import ReserveQuickActionButton from './ReserveQuickActionButton';
import ReserveStatusBadge from './ReserveStatusBadge';
import { formatReservationTime, getReservationGuestName, getReservationPrimaryNextAction, getReservationStatusLabel } from '@/lib/reservations/ui';
import { formatReservationDuration } from '@/lib/reservations/reservationFormatting';
import { getAssignedReservationResourceLabel, hasAssignedReservationResource } from '@/lib/reservations/floorSnapshot';
import { getReserveVocabulary, type ReserveVocabulary } from '@/lib/reservations/reserveVocabulary';
function value(v:any,f='—'){return v===undefined||v===null||v===''?f:String(v)}
export default function ReserveGuestDetails({reservation,onStatus,onAssign,onTableReady,updatingId,vocabulary}:{reservation:any;onStatus:(r:any,s:string)=>void;onAssign?:(r:any)=>void;onTableReady?:(r:any)=>void;updatingId?:string;vocabulary?:ReserveVocabulary}){const vocab=vocabulary||getReserveVocabulary(); if(!reservation)return <section className="reserve-card rounded-2xl p-4"><h2 className="text-lg font-black">{vocab.customer} Details</h2><p className="mt-4 reserve-muted">Select a reservation to view {vocab.customer.toLowerCase()} details.</p></section>; const action=getReservationPrimaryNextAction(reservation.status,vocab); const guestName=getReservationGuestName(reservation); const duration=reservation.duration_minutes||reservation.default_duration_minutes||reservation.reservation_duration_minutes||reservation.turn_time_minutes; const assigned=getAssignedReservationResourceLabel(reservation); const canTextReady=(reservation.status==='checked_in'||reservation.status==='arrived')&&hasAssignedReservationResource(reservation)&&reservation.customer_phone&&!reservation.table_ready_sms_sent; return <section className="reserve-card rounded-2xl p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h2 className="text-lg font-black">{vocab.customer} Details</h2><p className="mt-2 truncate text-2xl font-black">{guestName}</p><p className="mt-1 text-xs reserve-muted">{value(reservation.customer_phone,'No phone')} · {value(reservation.customer_email,'No email')}</p></div><ReserveStatusBadge status={reservation.status} label={getReservationStatusLabel(reservation.status,vocab)}/></div><dl className="mt-4 grid grid-cols-2 gap-2 text-sm"><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">{vocab.partySizeLabel}</dt><dd className="font-black">{value(reservation.party_size)}</dd></div><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">{vocab.resource}</dt><dd className="truncate font-black">{assigned}</dd></div><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">Time</dt><dd className="font-black">{formatReservationTime(reservation.reservation_time)} · {formatReservationDuration(duration)}</dd></div><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">Booked</dt><dd className="truncate font-black">{value(reservation.created_at||reservation.booked_at)}</dd></div></dl><div className="reserve-soft mt-3 rounded-xl p-3"><p className="text-[11px] font-black uppercase reserve-muted">Notes</p><p className="mt-1 text-sm">{reservation.special_request||reservation.notes||'No notes on this reservation.'}</p></div><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-3"><ReserveQuickActionButton disabled={updatingId===reservation.id || !!action.disabledReason || !action.targetStatus} title={action.disabledReason} onClick={()=>action.targetStatus&&onStatus(reservation,action.targetStatus)}>{updatingId===reservation.id?'Updating…':action.label}</ReserveQuickActionButton>{onTableReady&&<ReserveQuickActionButton disabled={!canTextReady||updatingId===reservation.id} title={!reservation.customer_phone?`Add a phone number before sending a ${vocab.readyAction.toLowerCase()} text.`:!canTextReady?`Check in and assign a ${vocab.resource.toLowerCase()} before sending ${vocab.readyAction.toLowerCase()}.`:undefined} onClick={()=>onTableReady(reservation)}>{reservation.table_ready_sms_sent?`${vocab.resource} ready sent`:vocab.readyAction}</ReserveQuickActionButton>}{onAssign&&<ReserveQuickActionButton onClick={()=>onAssign(reservation)}>{vocab.assignResource}</ReserveQuickActionButton>}<ReserveQuickActionButton disabled title="Move time is not wired in the current reservation backend.">Move time</ReserveQuickActionButton><ReserveQuickActionButton disabled title="Messaging beyond ready SMS is not wired in the current reservation backend.">Message</ReserveQuickActionButton><ReserveQuickActionButton onClick={()=>onStatus(reservation,'cancelled')}>Cancel</ReserveQuickActionButton><ReserveQuickActionButton onClick={()=>onStatus(reservation,'no_show')}>Mark no-show</ReserveQuickActionButton><ReserveQuickActionButton disabled={reservation.status!=='seated'} title={reservation.status!=='seated'?`${vocab.seatAction} before completing.`:undefined} onClick={()=>onStatus(reservation,'completed')}>{vocab.completedAction}</ReserveQuickActionButton></div></section>}
