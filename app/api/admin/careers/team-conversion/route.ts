import { NextResponse } from "next/server"; import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions"; import { supabaseAdmin } from "@/lib/supabase-admin";
export async function POST(req:Request){ try{ const admin=await requireAdminRole(ADMIN_PAGE_ACCESS.careersTeamConversion); const body=await req.json(); const {data}=await supabaseAdmin.from("career_team_conversions").insert({...body,converted_by:admin.user_id}).select("*").single(); return NextResponse.json({conversion:data,message:"Conversion record created. Link an existing user before changing platform permissions."}); }catch{return NextResponse.json({error:"We could not create this conversion record."},{status:500});}}
