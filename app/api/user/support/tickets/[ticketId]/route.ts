import{NextResponse}from"next/server";import{createClient}from"@/lib/supabase-server";import{supabaseAdmin}from"@/lib/supabase-admin";export async function GET(_:Request,{params}:{params:Promise<{ticketId:string}>}){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({success:false,error:"Unauthorized"},{status:401});const id=(await params).ticketId;const{data:t}=await supabaseAdmin.from("support_tickets").select("*").eq("id",id).maybeSingle();if(!t||(t.user_id!==user.id&&t.requester_email!==user.email&&t.email!==user.email))return NextResponse.json({success:false,error:"Not found"},{status:404});const{data:m}=await supabaseAdmin.from("support_ticket_messages").select("*").eq("ticket_id",id).eq("internal_note",false).order("created_at");return NextResponse.json({success:true,ticket:t,messages:m||[]})}
