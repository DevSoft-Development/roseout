import Link from "next/link";import CrmWorkspaceShell from "@/components/admin/crm/CrmWorkspaceShell";import { requireAdminRole } from "@/lib/admin-auth";import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions";import { listClaimCodes } from "@/lib/crm/core-modules";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireAdminRole(ADMIN_PAGE_ACCESS.claimQrs);const p=await searchParams,r=await listClaimCodes(p);return <CrmWorkspaceShell><main className="space-y-5 text-white"><header><p className="text-xs font-black uppercase tracking-[.25em] text-rose-300">Claims</p><h1 className="text-3xl font-black">Claim codes</h1><p className="text-white/60">Search, copy, print, expire, and audit claim invitations using the existing code table.</p></header><form className="grid gap-2 md:grid-cols-4"><input name="q" defaultValue={p.q} placeholder="Search code or URL" className="rounded-lg bg-black/40 p-2"/><select name="status" defaultValue={p.status||""} className="rounded-lg bg-black p-2"><option value="">All statuses</option>{["active","used","expired","revoked"].map(s=><option key={s}>{s}</option>)}</select><button className="rounded-lg bg-white font-black text-black">Apply</button><Link href="/admin/dashboard/crm/claims" className="rounded-lg border border-white/15 p-2 text-center">Claims queue</Link></form><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{r.rows.map((c:any)=><article key={c.id} className="rounded-2xl border border-white/10 bg-white/[.04] p-4"><b>{c.claim_code}</b><p className="text-sm text-white/55">{c.locations?.name||"Unassigned location"}</p><p className="text-xs text-white/45">Status {c.status||"active"} · expires {c.expires_at?new Date(c.expires_at).toLocaleDateString():"—"}</p><div className="mt-3 flex gap-2"><button className="rounded bg-white/10 px-3 py-2 text-xs">Copy</button><button className="rounded bg-white/10 px-3 py-2 text-xs">Print</button><button className="rounded bg-white/10 px-3 py-2 text-xs">Expire</button></div></article>)}</div>{!r.rows.length?<p className="rounded-2xl border border-dashed border-white/15 p-8 text-white/60">No claim codes match these filters.</p>:null}</main></CrmWorkspaceShell>}
