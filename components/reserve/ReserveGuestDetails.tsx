"use client";
import { FormEvent, useState } from 'react';
import ReserveQuickActionButton from './ReserveQuickActionButton';
import ReserveStatusBadge from './ReserveStatusBadge';
import { formatReservationTime, getReservationGuestName, getReservationPrimaryNextAction, getReservationStatusLabel } from '@/lib/reservations/ui';
import { formatReservationDuration } from '@/lib/reservations/reservationFormatting';
import { getAssignedReservationResourceLabel, hasAssignedReservationResource } from '@/lib/reservations/floorSnapshot';
import { getReserveVocabulary, type ReserveVocabulary } from '@/lib/reservations/reserveVocabulary';
function value(v:any,f='—'){return v===undefined||v===null||v===''?f:String(v)}
const templates=['Your reservation is confirmed.','Your table/space is ready.','We need a few more minutes.','Please reply if you need to cancel or change your time.'];
export default function ReserveGuestDetails({reservation,onStatus,onAssign,onTableReady,onRefresh,updatingId,vocabulary}:{reservation:any;onStatus:(r:any,s:string)=>void;onAssign?:(r:any)=>void;onTableReady?:(r:any)=>void;onRefresh?:()=>void;updatingId?:string;vocabulary?:ReserveVocabulary}){const vocab=vocabulary||getReserveVocabulary(); const [panel,setPanel]=useState<''|'move'|'message'>(''); const [notice,setNotice]=useState(''); const [busy,setBusy]=useState(false); if(!reservation)return <section className="reserve-card rounded-2xl p-4"><h2 className="text-lg font-black">{vocab.customer} Details</h2><p className="mt-4 reserve-muted">Select a reservation to view {vocab.customer.toLowerCase()} details.</p></section>; const action=getReservationPrimaryNextAction(reservation.status,vocab); const guestName=getReservationGuestName(reservation); const duration=reservation.duration_minutes||reservation.default_duration_minutes||reservation.reservation_duration_minutes||reservation.turn_time_minutes||90; const assigned=getAssignedReservationResourceLabel(reservation); const hasResource=hasAssignedReservationResource(reservation); const canTextReady=(reservation.status==='checked_in'||reservation.status==='waiting'||reservation.status==='arrived')&&hasResource&&reservation.customer_phone&&!reservation.table_ready_sms_sent; const canSeatFallback=(reservation.status==='checked_in'||reservation.status==='waiting'||reservation.status==='arrived')&&hasResource; const showPrimary=!(action.targetStatus==='seated'&&!canSeatFallback); async function submitMove(e:FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); setBusy(true); setNotice(''); try{const res=await fetch('/api/reserve/portal/reservations/update',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reservation_id:reservation.id,location_id:reservation.location_id,location_type:reservation.location_type,reservation_date:fd.get('date'),reservation_time:fd.get('time'),duration_minutes:Number(fd.get('duration')),special_request:fd.get('reason')})}); const data=await res.json(); if(!res.ok)throw new Error(data.error||'Reservation time could not be updated.'); setNotice('Reservation time updated.'); setPanel(''); onRefresh?.();}catch(err){setNotice(err instanceof Error?err.message:'Reservation time could not be updated.')}finally{setBusy(false)}} async function submitMessage(e:FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); setBusy(true); setNotice(''); try{const res=await fetch('/api/reserve/portal/reservations/message',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reservation_id:reservation.id,location_id:reservation.location_id,location_type:reservation.location_type,channel:fd.get('channel'),message:fd.get('message')})}); const data=await res.json(); if(!res.ok)throw new Error(data.error||'Message could not be sent.'); setNotice(data.message||'Message sent.'); setPanel(''); onRefresh?.();}catch(err){setNotice(err instanceof Error?err.message:'Message could not be sent.')}finally{setBusy(false)}} return <section className="reserve-card rounded-2xl p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h2 className="text-lg font-black">{vocab.customer} Details</h2><p className="mt-2 truncate text-2xl font-black">{guestName}</p><p className="mt-1 text-xs reserve-muted">{value(reservation.customer_phone,'No phone')} · {value(reservation.customer_email,'No email')}</p></div><ReserveStatusBadge status={reservation.status} label={getReservationStatusLabel(reservation.status,vocab)}/></div><dl className="mt-4 grid grid-cols-2 gap-2 text-sm"><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">{vocab.partySizeLabel}</dt><dd className="font-black">{value(reservation.party_size)}</dd></div><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">{vocab.resource}</dt><dd className="truncate font-black">{assigned}</dd></div><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">Time</dt><dd className="font-black">{formatReservationTime(reservation.reservation_time)} · {formatReservationDuration(duration)}</dd></div><div className="reserve-soft rounded-xl p-3"><dt className="text-[11px] reserve-muted">Booked</dt><dd className="truncate font-black">{value(reservation.created_at||reservation.booked_at)}</dd></div></dl><div className="reserve-soft mt-3 rounded-xl p-3"><p className="text-[11px] font-black uppercase reserve-muted">Notes</p><p className="mt-1 text-sm">{reservation.special_request||reservation.notes||'No notes on this reservation.'}</p></div>{notice&&<p className="mt-3 rounded-xl border border-white/10 bg-white/5 p-3 text-sm font-bold">{notice}</p>}<div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-3">{showPrimary&&<ReserveQuickActionButton disabled={updatingId===reservation.id || !!action.disabledReason || !action.targetStatus} title={action.disabledReason} onClick={()=>action.targetStatus&&onStatus(reservation,action.targetStatus)}>{updatingId===reservation.id?'Updating…':action.label}</ReserveQuickActionButton>}{!hasResource&&(reservation.status==='checked_in'||reservation.status==='waiting'||reservation.status==='arrived')&&<ReserveQuickActionButton onClick={()=>{setNotice(`${vocab.chooseResource} before seating this guest.`);onAssign?.(reservation)}}>{vocab.seatAction}</ReserveQuickActionButton>}{onTableReady&&<ReserveQuickActionButton disabled={!canTextReady||updatingId===reservation.id} title={!reservation.customer_phone?`Add a phone number before sending a ${vocab.readyAction.toLowerCase()} text.`:!canTextReady?`Check in and assign a ${vocab.resource.toLowerCase()} before sending ${vocab.readyAction.toLowerCase()}.`:undefined} onClick={()=>onTableReady(reservation)}>{reservation.table_ready_sms_sent?`${vocab.resource} ready sent`:vocab.readyAction}</ReserveQuickActionButton>}{onAssign&&reservation.status!=='seated'&&<ReserveQuickActionButton onClick={()=>onAssign(reservation)}>{vocab.assignResource}</ReserveQuickActionButton>}<ReserveQuickActionButton onClick={()=>setPanel(panel==='move'?'':'move')}>Move time</ReserveQuickActionButton><ReserveQuickActionButton onClick={()=>setPanel(panel==='message'?'':'message')}>Message</ReserveQuickActionButton><ReserveQuickActionButton onClick={()=>onStatus(reservation,'cancelled')}>Cancel</ReserveQuickActionButton>{reservation.status!=='seated'&&<ReserveQuickActionButton onClick={()=>onStatus(reservation,'no_show')}>Mark no-show</ReserveQuickActionButton>}</div>{panel==='move'&&<form onSubmit={submitMove} className="reserve-soft mt-4 grid gap-3 rounded-2xl p-4 sm:grid-cols-2"><label className="text-sm font-bold">Date<input name="date" type="date" required defaultValue={reservation.reservation_date} className="mt-1 w-full rounded-xl bg-black/20 px-3 py-2"/></label><label className="text-sm font-bold">Time<input name="time" type="time" required defaultValue={String(reservation.reservation_time||'').slice(0,5)} className="mt-1 w-full rounded-xl bg-black/20 px-3 py-2"/></label><label className="text-sm font-bold">Duration<input name="duration" type="number" min="15" step="15" defaultValue={duration} className="mt-1 w-full rounded-xl bg-black/20 px-3 py-2"/></label><label className="text-sm font-bold sm:col-span-2">Reason or note<textarea name="reason" rows={2} defaultValue={reservation.special_request||reservation.notes||''} className="mt-1 w-full rounded-xl bg-black/20 px-3 py-2"/></label><button disabled={busy} className="reserve-primary rounded-full px-4 py-2 font-black sm:col-span-2">{busy?'Saving…':'Update reservation time'}</button></form>}{panel==='message'&&<form onSubmit={submitMessage} className="reserve-soft mt-4 grid gap-3 rounded-2xl p-4"><p className="text-sm reserve-muted">To: {reservation.customer_phone||'No phone'} · {reservation.customer_email||'No email'}</p><select name="channel" defaultValue={reservation.customer_phone&&reservation.customer_email?'both':reservation.customer_phone?'sms':'email'} className="rounded-xl bg-black/20 px-3 py-2"><option value="sms">SMS</option><option value="email">Email</option><option value="both">SMS and email</option></select><div className="flex flex-wrap gap-2">{templates.map(t=><button key={t} type="button" onClick={(e)=>{const form=e.currentTarget.form; const textarea=form?.elements.namedItem('message') as HTMLTextAreaElement|null; if(textarea) textarea.value=t;}} className="rounded-full border border-white/10 px-3 py-1 text-xs font-bold">{t}</button>)}</div><textarea name="message" required rows={4} placeholder="Write a short TheOutHaven reservation message…" className="rounded-xl bg-black/20 px-3 py-2"/><button disabled={busy} className="reserve-primary rounded-full px-4 py-2 font-black">{busy?'Sending…':'Send message'}</button></form>}</section>}
