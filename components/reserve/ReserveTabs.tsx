export default function ReserveTabs({ tabs, active, onChange }: { tabs: { label: string; value: string; count?: number }[]; active: string; onChange?: (value: string) => void }) { return <div className="flex gap-2 overflow-x-auto pb-1">{tabs.map(t => <button key={t.value} type="button" onClick={() => onChange?.(t.value)} className={`shrink-0 rounded-full border px-4 py-2 text-sm font-black ${active===t.value ? "border-rose-400 bg-rose-600 text-white" : "border-white/10 bg-white/[0.06] text-white/65 hover:text-white"}`}>{t.label}{typeof t.count==="number" ? ` · ${t.count}` : ""}</button>)}</div>; }
