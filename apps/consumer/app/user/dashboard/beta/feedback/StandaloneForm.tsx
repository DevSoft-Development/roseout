"use client";import {useState} from "react";export default function Form({defaults}:{defaults:any}){const[m,setM]=useState('');async function sub(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch('/api/beta/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...Object.fromEntries(fd),assignmentId:defaults.assignmentId,page_url:defaults.route||location.href})});setM(r.ok?'Feedback submitted.':'Could not submit feedback.')}return <form onSubmit={sub} className="mt-5 grid gap-3"><input name="submitted_prompt" defaultValue={defaults.prompt||''} placeholder="Prompt" className="rounded-2xl bg-black/30 p-3"/><select name="feedback_type" className="rounded-2xl bg-black/30 p-3"><option>general</option><option>bad_search_results</option><option>search_was_slow</option><option>design_feedback</option></select><textarea name="expected_result" placeholder="What worked well?" className="rounded-2xl bg-black/30 p-3"/><textarea name="actual_result" placeholder="What felt off?" className="rounded-2xl bg-black/30 p-3"/><textarea name="message" required placeholder="Feedback" className="rounded-2xl bg-black/30 p-3"/><button className="rounded-full bg-rose-600 px-5 py-3 font-black">Submit Feedback</button>{m&&<p>{m}</p>}</form>}
