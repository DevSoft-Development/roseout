import type{SearchPlan}from"../planner/searchPlanTypes";import type{ScoredCandidate}from"../scoring/scoringTypes";import type{SearchPair}from"../pairing/pairingTypes";import type{SearchTrace}from"../observability/searchTrace";import type{ResolvedSearchResult,FallbackReason}from"./fallbackTypes";
export async function resolveFallback({plan,scored,pairs,retrievedCount,trace}:{plan:SearchPlan;scored:{restaurants:ScoredCandidate[];activities:ScoredCandidate[]};pairs:SearchPair[];retrievedCount:number;trace:SearchTrace}):Promise<ResolvedSearchResult>{const dual=scored.restaurants.filter(r=>scored.activities.some(a=>String(a.candidate.candidate.location.id)===String(r.candidate.candidate.location.id)));let fulfilled=plan.mode==="restaurant_only"?scored.restaurants.length>0:plan.mode==="activity_only"?scored.activities.length>0:plan.mode==="same_venue"?dual.length>0||(plan.fallback.allowNearbyPair&&pairs.length>0):plan.mode==="paired_outing"?pairs.length>0:scored.restaurants.length>0;let reason:FallbackReason|null=null;if(!fulfilled)reason=scored.restaurants.length&&!scored.activities.length?"partial_restaurants_only":scored.activities.length&&!scored.restaurants.length?"partial_activities_only":plan.pairing.required?"no_pairs_within_distance":"no_valid_results";else if(plan.mode==="same_venue"&&!dual.length&&pairs.length)reason="no_strong_same_venue_match";const partial=!fulfilled&&(scored.restaurants.length>0||scored.activities.length>0)&&plan.fallback.allowPartial;const used=reason!=null;trace.fallback={used,reason};return{requestedMode:plan.mode,resolvedMode:plan.mode,used,reason,requestFulfilled:fulfilled,partialResults:partial,restaurants:plan.pairing.required?[]:scored.restaurants.slice(0,20),activities:plan.pairing.required?[]:scored.activities.slice(0,20),sameVenueResults:dual.slice(0,20),pairs:pairs.slice(0,20),retrievedCandidates:retrievedCount};}
