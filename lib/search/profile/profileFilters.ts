export const PROFILE_PAGE_SIZES = [25, 50, 100, 250] as const;
export interface ProfileFilters { search?: string; status?: string; domain?: string; market?: string; state?: string; county?: string; borough?: string; city?: string; neighborhood?: string; category?: string; page: number; pageSize: number; sort: string; direction: "asc" | "desc"; }
export const isUuid = (value: string): boolean => /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value);
export function parseProfileFilters(params: URLSearchParams): ProfileFilters { const size = Number(params.get("pageSize")); const direction = params.get("direction"); return { search: params.get("search")?.trim() || undefined, status: params.get("status") || undefined, domain: params.get("domain") || undefined, market: params.get("market") || undefined, state: params.get("state") || undefined, county: params.get("county") || undefined, borough: params.get("borough") || undefined, city: params.get("city") || undefined, neighborhood: params.get("neighborhood") || undefined, category: params.get("category") || undefined, page: Math.max(1, Number(params.get("page")) || 1), pageSize: PROFILE_PAGE_SIZES.includes(size as typeof PROFILE_PAGE_SIZES[number]) ? size : 50, sort: params.get("sort") || "location_name", direction: direction === "asc" ? "asc" : "desc" }; }
export type ProfileStatus = "Current" | "Missing" | "Stale" | "Needs Review" | "Low Confidence" | "Refresh Queued" | "Refresh Failed" | "Not Eligible";
export function getSearchProfileStatus(input: { eligible: boolean; profileVersion?: number | null; currentVersion: number; needsReview?: boolean | null; confidence?: number | null; queueStatus?: string | null }): ProfileStatus { if (!input.eligible) return "Not Eligible"; if (input.queueStatus === "failed") return "Refresh Failed"; if (input.queueStatus === "queued" || input.queueStatus === "processing") return "Refresh Queued"; if (input.profileVersion == null) return "Missing"; if (input.profileVersion < input.currentVersion) return "Stale"; if (input.needsReview) return "Needs Review"; if ((input.confidence ?? 0) < .55) return "Low Confidence"; return "Current"; }
