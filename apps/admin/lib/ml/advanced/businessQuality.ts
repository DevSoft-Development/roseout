import { clampScore } from './scoring';
export function calculateBusinessQuality(l:any){ const missing:string[]=[]; const has=(k:string)=>Boolean(l?.[k]); ['address','phone','website','hours','primary_photo_url'].forEach(k=>{if(!has(k))missing.push(k)}); const profile=clampScore(100-missing.length*14); const dup=Number(l?.duplicate_risk_score||0); return { profile_completeness_score:profile, address_quality_score:has('address')?100:20, photo_readiness_score:has('primary_photo_url')?90:20, hours_quality_score:has('hours')?90:35, contact_quality_score:has('phone')?90:35, website_quality_score:has('website')?90:45, owner_claimed_score:l?.owner_user_id?100:0, reservation_readiness_score:l?.reservation_enabled?90:30, duplicate_risk_score:dup, public_search_ready_score:clampScore(profile-dup*.3), business_trust_score:clampScore(profile*.7+(has('address')?15:0)+(has('primary_photo_url')?15:0)-dup*.3), missing_fields:missing, recommended_actions:missing.map(f=>`Add ${f.replaceAll('_',' ')}`) }; }
export function calculatePublicSearchReadyScore(l:any){ return calculateBusinessQuality(l).public_search_ready_score; }
export function formatBusinessQualitySummary(f:any){ return f?`Trust score ${Math.round(f.business_trust_score||0)}; ${f.recommended_actions?.[0]||'profile looks search-ready'}.`:'No business quality data yet.'; }
