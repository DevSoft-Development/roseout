# Live-jazz Search Health follow-up

This follow-up wires the already-merged live-search classifier and row actions into the actual `RecentCreateSearchesPanel` used by `/admin/dashboard/search-health?tab=searches`.

The exact query `Romantic Italian dinner with live jazz in Manhattan tonight` must display as a mixed outing and an issue when activity results are missing. Every row must expose `Copy diagnostics` and `Mark incorrect result` actions.
