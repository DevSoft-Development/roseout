import type{SearchPlan}from"../planner/searchPlanTypes";export function validatePairDistance(plan:SearchPlan,distance:number|null,walking:number|null){if(plan.pairing.requireWalkable&&walking==null)return false;if(plan.pairing.maxWalkingMinutes!=null&&walking!=null&&walking>plan.pairing.maxWalkingMinutes)return false;if(plan.pairing.maxDistanceMiles!=null&&distance!=null&&distance>plan.pairing.maxDistanceMiles)return false;return true;}
