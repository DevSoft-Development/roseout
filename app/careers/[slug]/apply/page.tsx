import { notFound } from "next/navigation"; import { getCareerJobQuestions, getPublicCareerJobBySlug } from "@/lib/careers/queries"; import { requiresInternshipComplianceChecklist, isMarketingJob } from "@/lib/careers/format";
export default async function ApplyPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const job:any = await getPublicCareerJobBySlug(slug); if (!job || job.status !== "open") notFound(); const questions:any[] = await getCareerJobQuestions(job.id); return <main className="min-h-screen bg-[#050505] text-white"><section className="mx-auto max-w-3xl px-4 py-12"><p className="text-xs font-black uppercase tracking-[0.25em] text-rose-200">Apply to TheOutHaven</p><h1 className="mt-3 text-3xl font-black">{job.title}</h1><form action="/api/careers/apply" method="post" className="mt-8 grid gap-4 rounded-3xl border border-white/10 bg-[#101012] p-5"><input type="hidden" name="jobId" value={job.id}/><div className="grid gap-4 sm:grid-cols-2"><Field name="firstName" label="First name" required/><Field name="lastName" label="Last name" required/><Field name="email" label="Email" type="email" required/><Field name="phone" label="Phone"/><Field name="city" label="City"/><Field name="state" label="State"/></div><Field name="resumeUrl" label="Resume URL" placeholder="Link to your resume"/><Field name="linkedinUrl" label="LinkedIn URL"/><Field name="portfolioUrl" label="Portfolio URL"/><Field name="websiteUrl" label="Website URL"/><Field name="socialHandle" label="Social handle"/><label className="grid gap-2 text-sm font-bold">Cover letter / why interested<textarea name="coverLetter" className="min-h-32 rounded-xl border border-white/10 bg-black/30 p-3 text-white"/></label>{questions.map((q:any)=><label key={q.id} className="grid gap-2 text-sm font-bold">{q.question_label}<textarea name={`question_${q.id}`} required={q.required} className="min-h-24 rounded-xl border border-white/10 bg-black/30 p-3 text-white"/><input type="hidden" name={`question_label_${q.id}`} value={q.question_label}/></label>)}{isMarketingJob(job)?<div className="rounded-2xl bg-white/[0.04] p-4"><h2 className="font-black">Marketing / Social Links</h2><Field name="marketingPortfolioUrl" label="Additional content or social portfolio URL"/></div>:null}{requiresInternshipComplianceChecklist(job)?<div className="rounded-2xl border border-amber-300/20 bg-amber-500/10 p-4"><h2 className="font-black">School credit details</h2><p className="mt-1 text-xs text-white/65">Educational / college-credit internships may require school documentation.</p><Field name="schoolName" label="School name"/><Field name="programName" label="Program name"/><Field name="creditContactEmail" label="School contact email" type="email"/></div>:null}<label className="flex gap-3 text-sm text-white/70"><input name="terms" value="yes" type="checkbox" required/> I confirm the information is accurate and agree TheOutHaven may contact me about this application.</label><button className="rounded-xl bg-[#ec0b5b] px-5 py-3 text-sm font-black">Submit Application</button><p className="text-xs text-white/40">TODO: Add direct Supabase Storage upload when an approved resume bucket exists. For now, please provide resume and portfolio links.</p></form></section></main> }
function Field({label,name,type="text",required,placeholder}:{label:string;name:string;type?:string;required?:boolean;placeholder?:string}) { return <label className="grid gap-2 text-sm font-bold">{label}<input name={name} type={type} required={required} placeholder={placeholder} className="rounded-xl border border-white/10 bg-black/30 p-3 text-white placeholder:text-white/30"/></label> }
