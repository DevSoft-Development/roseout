"use client";

import { useState } from "react";
import { labelize } from "@/lib/team-tools-client";

export function AdminReviewButtons({ sessionId }: { sessionId: string }) {
  const [message, setMessage] = useState("");
  async function review(action: string) {
    const reason = action === "approve" ? "" : window.prompt("Reason or correction note") || "";
    const res = await fetch("/api/admin/team/work-sessions", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ sessionId, action, reason }) });
    const data = await res.json();
    setMessage(res.ok ? `${labelize(action)} saved.` : data.error || "Could not review session.");
    if (res.ok) window.location.reload();
  }
  return <div className="flex flex-wrap items-center gap-2"><button onClick={() => review("approve")} className="rounded-full bg-emerald-500 px-3 py-2 text-xs font-black text-black">Approve</button><button onClick={() => review("correction")} className="rounded-full bg-amber-400 px-3 py-2 text-xs font-black text-black">Correction</button><button onClick={() => review("reject")} className="rounded-full bg-red-500 px-3 py-2 text-xs font-black text-white">Reject</button>{message ? <span className="text-xs font-bold text-white/55">{message}</span> : null}</div>;
}

export function TeamMemberProfileForm({ users, profile }: { users: Array<{ id: string; email: string | null; name: string | null }>; profile?: any }) {
  const [message, setMessage] = useState("");
  const [teamType, setTeamType] = useState(profile?.team_type || "ambassador");
  async function submit(formData: FormData) {
    setMessage("Saving...");
    const allowedWorkTypes = Array.from(formData.getAll("allowedWorkTypes")).map(String);
    const body = { userId: formData.get("userId"), teamType, status: formData.get("status"), payType: formData.get("payType"), hourlyRate: formData.get("hourlyRate"), includeInPayroll: formData.get("includeInPayroll") === "on", canClockIn: formData.get("canClockIn") === "on", canTrackWork: formData.get("canTrackWork") === "on", canDoSiteVisits: formData.get("canDoSiteVisits") === "on", canDoSocialOutreach: formData.get("canDoSocialOutreach") === "on", canWorkSupportTickets: formData.get("canWorkSupportTickets") === "on", canUseDemoMode: formData.get("canUseDemoMode") === "on", allowedWorkTypes, notes: formData.get("notes") };
    const res = await fetch("/api/admin/team/members", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) });
    const data = await res.json();
    setMessage(res.ok ? "Team member saved." : data.error || "Could not save team member.");
    if (res.ok) window.location.reload();
  }
  const workTypes = ["field_visit","site_visit","social_outreach","phone_outreach","email_outreach","customer_support","owner_support","reservation_support","claim_support","listing_review","photo_review","quality_review","crm_cleanup","support_ticket","follow_up","email_follow_up","phone_follow_up","claim_code_delivery","qr_dropoff","owner_meeting","reservation_setup","reservation_demo","onboarding_support","team_review","payroll_review","proof_review","training","demo","admin_work","other"];
  return <form action={submit} className="rounded-[2rem] border border-white/10 bg-[#111] p-5 sm:p-7"><h2 className="text-2xl font-black">Add / edit team member</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><label className="text-sm font-bold text-white/65">User<select name="userId" defaultValue={profile?.user_id || ""} className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white"><option value="">Select user</option>{users.map((u) => <option key={u.id} value={u.id}>{u.name || u.email || u.id}</option>)}</select></label><label className="text-sm font-bold text-white/65">Team type<select value={teamType} onChange={(e) => setTeamType(e.target.value)} className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{["ambassador","experience_team","sales_team","support_team","manager","superadmin"].map((t) => <option key={t} value={t}>{labelize(t)}</option>)}</select></label><label className="text-sm font-bold text-white/65">Status<select name="status" defaultValue={profile?.status || "active"} className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{["active","inactive","suspended","training","archived"].map((s) => <option key={s}>{s}</option>)}</select></label><label className="text-sm font-bold text-white/65">Pay type<select name="payType" defaultValue={profile?.pay_type || "hourly"} className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white">{["hourly","commission","hourly_plus_commission","contractor","training_only","owner_or_training","unpaid"].map((s) => <option key={s}>{s}</option>)}</select></label><label className="text-sm font-bold text-white/65">Hourly rate<input name="hourlyRate" type="number" step="0.01" defaultValue={profile?.hourly_rate || ""} className="mt-2 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-white" /></label><label className="text-sm font-bold text-white/65 md:col-span-2">Notes<textarea name="notes" defaultValue={profile?.notes || ""} className="mt-2 w-full rounded-2xl border border-white/10 bg-black px-4 py-3 text-white" /></label></div><div className="mt-5 grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{[["includeInPayroll","Include in payroll"],["canClockIn","Can clock in"],["canTrackWork","Can track work"],["canDoSiteVisits","Site visits"],["canDoSocialOutreach","Social outreach"],["canWorkSupportTickets","Support tickets"],["canUseDemoMode","Demo mode"]].map(([name,label]) => <label key={name} className="rounded-2xl border border-white/10 bg-black/30 p-3 text-sm font-bold"><input name={name} type="checkbox" defaultChecked={profile ? Boolean(profile[name.replace(/[A-Z]/g, (m) => `_${m.toLowerCase()}`)]) : !["includeInPayroll"].includes(name)} /> {label}</label>)}</div><div className="mt-5"><p className="text-xs font-black uppercase tracking-[0.2em] text-white/40">Allowed work types</p><p className="mt-1 text-xs font-bold text-white/45">Leave all unchecked to use default work types for this team type.</p><div className="mt-3 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{workTypes.map((type) => <label key={type} className="rounded-2xl border border-white/10 bg-black/30 p-3 text-xs font-bold"><input type="checkbox" name="allowedWorkTypes" value={type} defaultChecked={profile?.allowed_work_types?.includes(type)} /> {labelize(type)}</label>)}</div></div><button className="mt-5 rounded-full bg-white px-6 py-3 text-sm font-black text-black">Save team member</button>{message ? <p className="mt-3 text-sm font-bold text-white/60">{message}</p> : null}</form>;
}

export function SupportTicketWorkButtons({ ticketId }: { ticketId: string }) {
  const [message, setMessage] = useState("");
  async function act(action: string) { const res = await fetch("/api/team/support-work", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ticketId, action }) }); const data = await res.json(); setMessage(res.ok ? `${labelize(action)} logged.` : data.error || "Could not log work."); if (res.ok && ["closed","resolved","marked_complete","reopened"].includes(action)) window.location.reload(); }
  return <div className="rounded-3xl border border-white/10 bg-black/35 p-4"><p className="text-xs font-black uppercase tracking-[0.2em] text-white/40">Team work tracking</p><p className="mt-2 text-sm font-bold text-white/55">Support work is remote: no GPS and no proof photo required.</p><div className="mt-3 flex flex-wrap gap-2">{["start","stop","answered","marked_complete","resolved","closed","reopened"].map((a) => <button key={a} onClick={() => act(a)} className="rounded-full border border-white/10 bg-white/[0.08] px-3 py-2 text-xs font-black text-white hover:bg-white/15">{labelize(a)}</button>)}</div>{message ? <p className="mt-3 text-sm font-bold text-white/60">{message}</p> : null}</div>;
}
