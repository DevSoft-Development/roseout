"use client";
import { useState } from "react";
import type { KnowledgeBaseCategory } from "@/lib/knowledge-base/types";
export default function CategoriesClient({ categories }: { categories: KnowledgeBaseCategory[] }) { const [items,setItems]=useState(categories); const [draft,setDraft]=useState({name:"",slug:"",description:"",audience:"internal",sort_order:0,is_active:true}); async function save(){ const res=await fetch('/api/admin/knowledge-base/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(draft)}); const data=await res.json(); if(!res.ok){alert(data.error);return;} setItems([...items.filter(i=>i.id!==data.category.id),data.category].sort((a,b)=>a.sort_order-b.sort_order)); setDraft({name:"",slug:"",description:"",audience:"internal",sort_order:0,is_active:true}); } return <div className="space-y-5"><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-5 grid gap-3 md:grid-cols-6"><input placeholder="Name" value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})} className="rounded-2xl bg-black/30 p-3 md:col-span-2"/><input placeholder="Slug" value={draft.slug} onChange={e=>setDraft({...draft,slug:e.target.value})} className="rounded-2xl bg-black/30 p-3"/><select value={draft.audience} onChange={e=>setDraft({...draft,audience:e.target.value})} className="rounded-2xl bg-[#120b0a] p-3"><option>internal</option><option>public</option><option>both</option></select><input type="number" value={draft.sort_order} onChange={e=>setDraft({...draft,sort_order:Number(e.target.value)})} className="rounded-2xl bg-black/30 p-3"/><button onClick={save} className="rounded-2xl bg-amber-200 p-3 font-black text-[#32130f]">Save</button><input placeholder="Description" value={draft.description} onChange={e=>setDraft({...draft,description:e.target.value})} className="rounded-2xl bg-black/30 p-3 md:col-span-6"/></div><div className="grid gap-3">{items.map(c=><div key={c.id} className="rounded-3xl border border-white/10 bg-white/[0.04] p-5 flex justify-between"><div><b>{c.name}</b><p className="text-sm text-rose-100/60">/{c.slug} · {c.audience} · sort {c.sort_order}</p></div><button onClick={()=>setDraft({name:c.name,slug:c.slug,description:c.description ?? "",audience:c.audience,sort_order:c.sort_order,is_active:c.is_active})} className="text-amber-100 font-black">Edit</button></div>)}</div></div> }
