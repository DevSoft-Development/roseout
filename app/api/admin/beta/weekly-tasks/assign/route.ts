import { NextResponse } from "next/server";import { requireAdminApiRole } from "@/lib/admin-api-auth";import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions";import { supabaseAdmin } from "@/lib/supabaseAdmin";import { assignWeeklyBetaTasksForAllActiveTesters } from "@/lib/beta/weeklyTasks";
export async function POST(){const a=await requireAdminApiRole(ADMIN_PAGE_ACCESS.giveawayManage);if(a.error)return a.error;const results=await assignWeeklyBetaTasksForAllActiveTesters();const assigned=results.reduce((s:any,r:any)=>s+Number(r.assigned||0),0);await supabaseAdmin.from("admin_audit_logs").insert({actor_user_id:a.adminUser?.user_id??null,action:"weekly_task_assigned_all",entity_type:"beta_task_assignment",summary:"Assigned weekly tasks to active beta testers",metadata:{assigned,testers:results.length}});return NextResponse.json({success:true,assigned,testers:results.length,results});}
