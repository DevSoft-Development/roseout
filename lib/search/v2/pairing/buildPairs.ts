import type{SearchPlan}from"../planner/searchPlanTypes";import type{ScoredCandidate}from"../scoring/scoringTypes";import type{SearchTrace}from"../observability/searchTrace";import type{SearchPair}from"./pairingTypes";import{validatePairDistance}from"./validatePairDistance";import{haversineMiles}from"../../enterprise/distance";
function coords(c:ScoredCandidate){const l=c.candidate.candidate.location;const lat=Number(l.latitude),lng=Number(l.longitude);return Number.isFinite(lat)&&Number.isFinite(lng)?{lat,lng}:null;}
export async function buildPairs({plan,restaurants,activities,trace}:{plan:SearchPlan;restaurants:ScoredCandidate[];activities:ScoredCandidate[];trace?:SearchTrace}):Promise<SearchPair[]>{const pairs:SearchPair[]=[];for(const restaurant of restaurants.slice(0,20))for(const activity of activities.slice(0,20)){const r=coords(restaurant),a=coords(activity);const same=String(restaurant.candidate.candidate.location.id)===String(activity.candidate.candidate.location.id);const distance=same?0:r&&a?haversineMiles(r.lat,r.lng,a.lat,a.lng):null;const walking=distance==null?null:Math.ceil(distance*20);if(plan.pairing.sameVenueRequired&&!same)continue;if(!validatePairDistance(plan,distance,walking))continue;const distanceScore=distance==null?40:Math.max(0,100-distance*25);const mlPairBoost=Math.min(5,Number(restaurant.candidate.candidate.location.ml_pair_score??activity.candidate.candidate.location.ml_pair_score??0));const total=(restaurant.scores.total+activity.scores.total)*.4+distanceScore*.2+mlPairBoost;pairs.push({restaurant,activity,distanceMiles:distance,walkingMinutes:walking,walkingMinutesSource:walking==null?"unavailable":"estimated",scores:{restaurant:restaurant.scores.total,activity:activity.scores.total,distance:distanceScore,combinedQuality:(restaurant.scores.quality+activity.scores.quality)/2,sequence:100,mlPairBoost,total},reasons:[same?"both roles at one venue":"roles satisfy requested outing",walking==null?"walking time unavailable":`about ${walking} minutes walking`]});}pairs.sort((a,b)=>b.scores.total-a.scores.total);if(trace){trace.counts.pairsBuilt=pairs.length;trace.counts.pairsValid=pairs.length;}return pairs.slice(0,20);}
