"use client";import {useState} from "react";export default function AccountClient({profile,user}:{profile:any;user:any}){const [f,setF]=useState({full_name:profile?.full_name||'',preferred_name:profile?.preferred_name||'',zip_code:profile?.zip_code||'',phone:profile?.phone||profile?.mobile_number||''});const [m,setM]=useState('');async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/user/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setM(r.ok?'Account saved.':'Could not save account.')}return <form onSubmit={save} className="grid gap-3"><p className="text-sm text-white/55">Email: <b className="text-white">{user.email}</b></p>{Object.entries(f).map(([k,v])=><input key={k} className="min-h-12 rounded-2xl border border-white/10 bg-black/30 px-4 text-white" placeholder={k.replace('_',' ')} value={v} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<button className="rounded-full bg-rose-600 px-5 py-3 text-sm font-black">Save Account</button>{m&&<p className="text-sm text-emerald-100">{m}</p>}</form>}
