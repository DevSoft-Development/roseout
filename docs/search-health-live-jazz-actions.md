# Row actions

`Copy diagnostics` copies the complete search row payload needed for investigation. `Mark incorrect result` sends the existing Search Health review PATCH request and records the row as reviewing.
