import { requireAdminRole } from "@theouthaven/auth/admin-session";import { requestRunCancellation } from "@/lib/search/profile/profileRunRepository";import { NextResponse } from "next/server";export async function POST(_r:Request,{params}:{params:Promise<{runId:string}>}){await requireAdminRole(["superadmin", "admin"]);try{return NextResponse.json({run:await requestRunCancellation((await params).runId)});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Cancellation failed"},{status:500});}}
