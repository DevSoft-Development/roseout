# Pull request note

The predecessor PR merged the detector and reusable action component. This follow-up completes the missing integration in `RecentCreateSearchesPanel`, which is the component actually rendered by the `searches` tab.
