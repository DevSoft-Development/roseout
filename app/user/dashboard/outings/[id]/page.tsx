import Link from "next/link";import { redirect } from "next/navigation";import UserDashboardShell,{DashboardCard} from "@/components/user/UserDashboardShell";import { requireUserForDashboard,getUserBetaStatus } from "@/lib/user-dashboard";import { supabaseAdmin } from "@/lib/supabaseAdmin";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await requireUserForDashboard(`/user/dashboard/outings/${id}`);const beta=await getUserBetaStatus(user.id,user.email);const {data:o}=await supabaseAdmin.from('user_outings').select('*').eq('id',id).eq('user_id',user.id).maybeSingle();if(!o)redirect('/user/dashboard/outings');return <UserDashboardShell isBeta={Boolean(beta)}><Link href="/user/dashboard/outings" className="text-sm font-bold text-rose-100">← Back to outings</Link><DashboardCard className="mt-5"><p className="text-xs font-black uppercase tracking-[.25em] text-rose-200">{o.status||'Booked'} Outing</p><h1 className="mt-3 text-4xl font-black">{o.title||'TheOutHaven Outing'}</h1><div className="mt-6 grid gap-4 md:grid-cols-2"><div className="rounded-2xl bg-black/30 p-4"><h2 className="font-black">Restaurant</h2><p className="mt-2 text-white/65">{o.restaurant_name||'TBD'}</p><p className="text-sm text-white/45">{o.restaurant_address}</p>{o.restaurant_url?<a className="mt-3 inline-flex text-sm font-bold text-rose-100" href={o.restaurant_url}>Open link</a>:null}</div><div className="rounded-2xl bg-black/30 p-4"><h2 className="font-black">Activity</h2><p className="mt-2 text-white/65">{o.activity_name||'TBD'}</p><p className="text-sm text-white/45">{o.activity_address}</p></div></div><pre className="mt-6 overflow-auto rounded-2xl bg-black/40 p-5 text-xs text-white/70">{JSON.stringify({plan:o.plan_payload,reservation:o.reservation_payload},null,2)}</pre><Link href="/support" className="mt-5 inline-flex rounded-full border border-white/15 px-4 py-2 text-xs font-black">Contact support</Link></DashboardCard></UserDashboardShell>}
