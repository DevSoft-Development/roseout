import { describe, expect, it } from "vitest";
import { forecastTotals, opportunityHealth, requiredDiscountRole, stageAgeDays, validateTransition, weightedAmount } from "..";
const base:any={id:"1",account_id:"a",name:"Deal",pipeline_key:"reserve_pro",stage:"proposal",status:"open",owner_user_id:"u",assigned_team:null,amount:1000,probability:60,forecast_category:"pipeline",expected_close_date:"2030-01-01",next_step:"Review",next_step_at:null,primary_contact_id:"c",primary_location_id:"l",loss_reason:null,proposal_url:"https://example.com",last_stage_changed_at:"2026-01-01T00:00:00Z",version:1};
describe("Phase 3 opportunity rules",()=>{it("calculates weighted values centrally",()=>expect(weightedAmount(999.99,35)).toBe(350));it("enforces pipeline compatibility",()=>expect(()=>validateTransition(base,"claim_sent")).toThrow(/not valid/));it("enforces explicit transition maps",()=>expect(()=>validateTransition(base,"closed_won")).toThrow(/not allowed/));it("requires a reason for overrides",()=>expect(()=>validateTransition(base,"closed_won",{override:true,productCount:1})).toThrow(/reason/));it("requires loss reason",()=>expect(()=>validateTransition({...base,stage:"payment_pending"},"closed_lost")).toThrow(/loss reason/));it("calculates forecast categories independently",()=>expect(forecastTotals([{...base,forecast_category:"commit"}])).toMatchObject({pipeline:1000,weighted:600,commit:1000}));it("provides explainable health",()=>expect(opportunityHealth({...base,next_step:null,primary_contact_id:null},30,new Date("2026-01-05")).reasons).toEqual(["No next step","No primary contact"]));it("calculates stage age",()=>expect(stageAgeDays("2026-01-01",new Date("2026-01-11"))).toBe(10));it.each([[5,"owner"],[15,"manager"],[25,"admin"]])("requires correct discount role",(percent,role)=>expect(requiredDiscountRole(percent)).toBe(role));});
