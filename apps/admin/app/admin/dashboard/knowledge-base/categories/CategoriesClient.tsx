"use client";import { useState } from "react";export default function CategoriesClient({categories}:{categories:any[]}){const[items,setItems]=useState(categories);const[draft,setDraft]=useState({id:"",name:"",slug:"",description:"",audience:"internal",sort_order:0,is_active:true});async function save(){const res=await fetch("/api/admin/knowledge-base/categories",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(draft)});const data=await res.json();if(!res.ok){alert(data.error||"Save failed");return}setItems([...items.filter(x=>x.id!==data.category.id),data.category].sort((a,b)=>a.sort_order-b.sort_order));setDraft({id:"",name:"",slug:"",description:"",audience:"internal",sort_order:0,is_active:true})}return <div className="space-y-5"><div className="grid gap-3 rounded-3xl border border-white/10 bg-white/[0.04] p-5 md:grid-cols-6"><input value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})} placeholder="Name" className="rounded-xl bg-black/40 p-3 md:col-span-2"/><input value={draft.slug} onChange={e=>setDraft({...draft,slug:e.target.value})} placeholder="Slug" className="rounded-xl bg-black/40 p-3"/><select value={draft.audience} onChange={e=>setDraft({...draft,audience:e.target.value})} className="rounded-xl bg-black/40 p-3"><option>internal</option><option>public</option><option>both</option></select><input type="number" value={draft.sort_order} onChange={e=>setDraft({...draft,sort_order:Number(e.target.value)})} className="rounded-xl bg-black/40 p-3"/><button onClick={save} className="rounded-xl bg-rose-600 font-black">Save</button><input value={draft.description} onChange={e=>setDraft({...draft,description:e.target.value})} placeholder="Description" className="rounded-xl bg-black/40 p-3 md:col-span-6"/></div><div className="grid gap-3">{items.map(c=><button key={c.id} onClick={()=>setDraft({...c,description:c.description||""})} className="rounded-2xl border border-white/10 bg-white/[0.03] p-4 text-left"><b>{c.name}</b><span className="ml-2 text-xs text-white/50">/{c.slug} · {c.audience}</span></button>)}</div></div>}
