import { activityRetrievalTerms } from "../taxonomy";
import type { SearchPlan } from "../planner/searchPlanTypes";
import type { RetrievalRequest } from "./retrievalTypes";
export function buildRetrievalRequests(plan: SearchPlan): RetrievalRequest[] { const requests: RetrievalRequest[] = []; if (plan.restaurant.required) requests.push({ desiredRole:"restaurant", cuisines:plan.restaurant.cuisines, foods:plan.restaurant.foods, categories:[], features:plan.restaurant.features, retrievalTerms:[...plan.restaurant.cuisines,...plan.restaurant.foods,...plan.restaurant.mealPeriods,"restaurant"], eligibleStorageTypes:["restaurant","activity","nightlife"],geo:plan.geo }); if (plan.activity.required) for (const category of plan.activity.categories.length ? plan.activity.categories : ["general"]) requests.push({desiredRole:`${category}_activity`,cuisines:[],foods:[],categories:[category],features:plan.activity.features,retrievalTerms:category === "general" ? ["activity"] : activityRetrievalTerms(category),eligibleStorageTypes:["activity","restaurant","nightlife"],geo:plan.geo}); return requests.slice(0,2); }
