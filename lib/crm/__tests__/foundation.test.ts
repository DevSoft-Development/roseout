import { describe,expect,it } from "vitest";import { CRM_PIPELINES,validateStage } from "../pipelines";import { isTaskOverdue,normalizeEmail,normalizeLifecycle,validateTaskTransition } from "../validation";import { selectAccountMatch } from "../migrations/account-backfill";
describe("CRM foundation",()=>{it("normalizes contact email",()=>expect(normalizeEmail(" Owner@Example.COM ")).toBe("owner@example.com"));it("validates lifecycle",()=>{expect(normalizeLifecycle("customer")).toBe("customer");expect(()=>normalizeLifecycle("paid")).toThrow()});it("keeps pipeline stages distinct",()=>{expect(CRM_PIPELINES.reserve_pro).toContain("demo_scheduled");expect(CRM_PIPELINES.business_claim).not.toContain("demo_scheduled");expect(()=>validateStage("business_claim","demo_scheduled")).toThrow();expect(()=>validateStage("reserve_pro","closed_lost")).toThrow(/loss reason/)});it("calculates overdue work",()=>{expect(isTaskOverdue("2020-01-01T00:00:00Z","open",new Date("2021-01-01"))).toBe(true);expect(isTaskOverdue("2020-01-01T00:00:00Z","completed",new Date("2021-01-01"))).toBe(false)});it("guards task transitions",()=>expect(()=>validateTaskTransition("cancelled","completed")).toThrow());it("uses deterministic strongest backfill evidence",()=>expect(selectAccountMatch({organizationId:"org-1",ownerUserId:"user-1",locationId:"loc-1"})).toEqual({key:"organization:org-1",strategy:"trusted_organization",confidence:"high"}))});

