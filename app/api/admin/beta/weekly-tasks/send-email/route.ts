import { NextResponse } from "next/server";import { requireAdminApiRole } from "@/lib/admin-api-auth";import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions";import { supabaseAdmin } from "@/lib/supabaseAdmin";import { sendBetaRemindersForActiveTesters } from "@/lib/beta/reminderEmails";
export async function POST(){const a=await requireAdminApiRole(ADMIN_PAGE_ACCESS.giveawayManage);if(a.error)return a.error;const results=await sendBetaRemindersForActiveTesters("weekly_tasks");const sent=results.filter((r:any)=>r.status==="sent").length;await supabaseAdmin.from("admin_audit_logs").insert({actor_user_id:a.adminUser?.user_id??null,action:"weekly_task_email_sent",entity_type:"beta_email_reminder",summary:"Weekly beta task email sent",metadata:{sent,total:results.length}});return NextResponse.json({success:true,sent,total:results.length,results});}
