import { NextRequest, NextResponse } from "next/server";
import { supabaseAdmin } from "@/lib/supabaseAdmin";
import { createInviteCode } from "@/lib/beta/weeklyTasks";
import { requireBetaAdmin, safeError } from "../_shared";
export async function GET(){const a=await requireBetaAdmin(); if(a.error)return a.error; const {data,error}=await supabaseAdmin.from("beta_applications").select("*").order("created_at",{ascending:false}).limit(300); if(error)return safeError(); return NextResponse.json({success:true,applications:data||[]});}
export async function PATCH(req:NextRequest){const a=await requireBetaAdmin(); if(a.error)return a.error; try{const b=await req.json(); const status=String(b.status||""); const id=String(b.id||""); if(!id||!status)return safeError("id and status required",400); const {data:app}=await supabaseAdmin.from("beta_applications").update({status,reviewed_by:a.adminUser?.user_id,reviewed_at:new Date().toISOString()}).eq("id",id).select("*").single(); if(status==="approved"&&app){await supabaseAdmin.from("beta_testers").upsert({application_id:app.id,name:app.name,email:app.email,phone:app.phone,tester_type:app.tester_type,status:"active",weekly_required_tests:5,invite_code:createInviteCode(),approved_by:a.adminUser?.user_id,approved_at:new Date().toISOString()},{onConflict:"email"});} return NextResponse.json({success:true,application:app});}catch(error){console.error("ADMIN_BETA_APP_PATCH",error);return safeError();}}
