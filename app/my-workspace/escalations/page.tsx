import { WorkspaceListPage } from "@/components/WorkspaceListPage"; import { ensureTeamProfileForCurrentUser } from "@/lib/team-tools"; import { supabaseAdmin } from "@/lib/supabase-admin";
export const dynamic="force-dynamic";
export default async function Escalations(){ const {user}=await ensureTeamProfileForCurrentUser(); const {data=[]}=await supabaseAdmin.from("workspace_escalations").select("*").or(`created_by_user_id.eq.${user.id},assigned_to_user_id.eq.${user.id}`).order("created_at",{ascending:false}).limit(100); return <WorkspaceListPage title="Escalations" backHref="/my-workspace" description="Owner-angry, wrong-business-info, claim, reservation, billing, legal/privacy, repeated failed outreach, do-not-contact, and other escalations." rows={data||[]} empty="No escalations."/> }
