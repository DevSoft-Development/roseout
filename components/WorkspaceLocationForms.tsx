"use client";
import { useState } from "react";
export function LocationChangeRequestForm({ locationId }: { locationId: string }) { const [message,setMessage]=useState(""); async function submit(formData:FormData){ const res=await fetch("/api/workspace/location-change-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({locationId,fieldName:formData.get("fieldName"),requestedValue:formData.get("requestedValue"),reason:formData.get("reason")})}); const data=await res.json(); setMessage(res.ok?"Change request submitted for manager review.":data.error||"Could not submit request."); if(res.ok) window.location.reload(); } return <form action={submit} className="rounded-3xl border border-white/10 bg-[#111] p-5"><h2 className="text-xl font-black">Request protected field change</h2><select name="fieldName" className="mt-4 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-sm font-bold text-white">{["owner_email","phone","website","address","latitude","longitude","claim_status","public_visibility_tier","is_searchable","quality_status","published","slug","billing_settings"].map(f=><option key={f}>{f}</option>)}</select><input name="requestedValue" placeholder="Requested value" className="mt-3 w-full rounded-full border border-white/10 bg-black px-4 py-3 text-sm font-bold text-white"/><textarea name="reason" required placeholder="Reason / support context" className="mt-3 w-full rounded-2xl border border-white/10 bg-black px-4 py-3 text-sm font-bold text-white"/><button className="mt-3 rounded-full bg-white px-5 py-3 text-sm font-black text-black">Submit request</button>{message?<p className="mt-3 text-sm font-bold text-white/60">{message}</p>:null}</form> }
