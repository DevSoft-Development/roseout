import "server-only";
export function rateLimitRetryAt(sentAt:Date[],now=new Date(),daily=1,weekly=3):Date|null {const day=sentAt.filter(x=>x>new Date(now.getTime()-86_400_000)).sort((a,b)=>a.getTime()-b.getTime());const week=sentAt.filter(x=>x>new Date(now.getTime()-7*86_400_000)).sort((a,b)=>a.getTime()-b.getTime());const candidates:Date[]=[];if(day.length>=daily&&daily>0)candidates.push(new Date(day[day.length-daily].getTime()+86_400_000));if(week.length>=weekly&&weekly>0)candidates.push(new Date(week[week.length-weekly].getTime()+7*86_400_000));return candidates.length?new Date(Math.max(...candidates.map(x=>x.getTime()))):null}
export function nextAllowedAfterQuietHours(now:Date,timeZone:string,startHour=20,endHour=8):Date|null {const parts=new Intl.DateTimeFormat("en-US",{timeZone,hour:"2-digit",hour12:false}).formatToParts(now);const hour=Number(parts.find(x=>x.type==="hour")?.value);if(hour>=endHour&&hour<startHour)return null;const delayHours=hour>=startHour?24-hour+endHour:endHour-hour;return new Date(now.getTime()+delayHours*3_600_000)}

