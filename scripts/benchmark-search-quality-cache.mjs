import { performance } from "node:perf_hooks";
const samples=Number(process.env.SEARCH_BENCHMARK_SAMPLES||1000);const values=[];const cache=new Map();for(let i=0;i<samples;i++){const start=performance.now();const key=`query-${i%15}`;if(!cache.has(key))cache.set(key,{rank:Array.from({length:50},(_,n)=>n).sort((a,b)=>b-a)});cache.get(key);values.push(performance.now()-start)}values.sort((a,b)=>a-b);const at=p=>values[Math.min(values.length-1,Math.floor(values.length*p))];console.log(JSON.stringify({samples,p50_ms:at(.5),p95_ms:at(.95),max_ms:at(1),note:"synthetic, no production writes or external APIs"},null,2));
