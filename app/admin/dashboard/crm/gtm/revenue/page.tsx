import Link from 'next/link';
import CrmWorkspaceShell from '@/components/admin/crm/CrmWorkspaceShell';
import { requireAdminRole } from '@/lib/admin-auth';
import { CRM_READ_ROLES } from '@/lib/crm/permissions';
import { getGtmRevenueIntelligence } from '@/lib/gtm/revenue';
export const dynamic='force-dynamic';
export default async function GtmRevenuePage(){await requireAdminRole(CRM_READ_ROLES); const r=await getGtmRevenueIntelligence(); return <CrmWorkspaceShell><main className="space-y-5 text-white"><header className="flex items-end justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.2em] text-rose-300">GTM Revenue</p><h1 className="mt-1 text-3xl font-black">Acquisition Economics</h1><p className="mt-1 text-white/55">Paid subscriptions only. Claimed businesses do not count as revenue.</p></div><Link href="/admin/dashboard/crm/gtm" className="rounded-xl border border-white/10 px-4 py-2 text-sm font-black">GTM</Link></header><section className="grid grid-cols-2 gap-3 lg:grid-cols-4">{[['Paid customers',r.customers],['MRR',`$${r.mrr.toLocaleString()}`],['ARR',`$${r.arr.toLocaleString()}`],['Observed CAC',r.cac===null?'—':`$${r.cac}`]].map(([a,b])=><div key={String(a)} className="rounded-2xl border border-white/10 bg-white/[.04] p-4"><b className="text-2xl">{b}</b><small className="mt-1 block text-white/50">{a}</small></div>)}</section><section className="rounded-3xl border border-white/10 bg-[#0e0e11] p-5"><h2 className="text-xl font-black">Channel attribution</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead className="text-zinc-500"><tr><th className="pb-3">Channel</th><th>Spend</th><th>Customers</th><th>MRR</th><th>CAC</th></tr></thead><tbody>{r.channels.map((x:any)=><tr key={x.channel} className="border-t border-white/10"><td className="py-3 font-black">{x.channel}</td><td>${Number(x.spend).toLocaleString()}</td><td>{x.customers}</td><td>${Number(x.mrr).toLocaleString()}</td><td>{x.cac===null?'—':`$${x.cac}`}</td></tr>)}</tbody></table></div><p className="mt-4 text-xs text-zinc-500">LTV is intentionally reported as observed-only until enough retention and churn history exists for defensible cohort LTV.</p></section></main></CrmWorkspaceShell>}
