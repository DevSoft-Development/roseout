import type{SearchPlan}from"../planner/searchPlanTypes";import type{ResolvedSearchResult}from"../fallback/fallbackTypes";import type{SearchTrace}from"../observability/searchTrace";import type{PublicLocationCard,PublicSearchResponseV2}from"./responseTypes";import{resultCounts}from"./resultCounts";
const card=(x:ResolvedSearchResult["restaurants"][number]):PublicLocationCard=>({...x.candidate.candidate.location,searchRole:x.selectedRole,searchScore:x.scores.total});
export function buildPublicSearchResponse({plan,result,trace}:{plan:SearchPlan;result:ResolvedSearchResult;trace:SearchTrace}):PublicSearchResponseV2{const restaurants=result.restaurants.map(card),activities=result.activities.map(card),sameVenueResults=result.sameVenueResults.map(card),pairs=result.pairs.map(p=>({restaurant:card(p.restaurant),activity:card(p.activity),distanceMiles:p.distanceMiles,walkingMinutes:p.walkingMinutes,score:p.scores.total}));const displayMode=pairs.length?"pairs":sameVenueResults.length?"same_venue_cards":result.partialResults?"partial_mixed":restaurants.length?"restaurant_cards":activities.length?"activity_cards":"empty";return{version:"public-search-v2",success:result.requestFulfilled||result.partialResults,requestFulfilled:result.requestFulfilled,partialResults:result.partialResults,requestId:plan.requestId,requestedMode:plan.mode,resolvedMode:result.resolvedMode,displayMode,restaurants,activities,sameVenueResults,pairs,counts:resultCounts(result),fallback:{used:result.used,reason:result.reason},message:result.requestFulfilled?"We found options matching your outing.":result.partialResults?"We found partial matches and clearly labeled them.":"No valid matches were found within your constraints.",timing:trace.timing,ml:{enabled:trace.ml.enabled,modelVersion:trace.ml.modelVersion,rankingVariant:trace.ml.rankingVariant,rolloutBucket:trace.ml.rolloutBucket}};}
