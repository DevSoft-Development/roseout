import type { EnterpriseLocation } from "../../enterprise/types"; import type { RoleEvidence } from "./roleTypes";
const authoritative=["cuisine","cuisine_type","activity_type","primary_category"];const strong=["name","restaurant_name","activity_name","tags","semantic_tags","intent_tags","best_for_tags","date_style_tags","google_types"];const supporting=["search_document","search_keywords","semantic_search_text","description"];
export function collectRoleEvidence(location:EnterpriseLocation,terms:readonly string[]):RoleEvidence[]{const out:RoleEvidence[]=[];for(const [strength,fields] of [["authoritative",authoritative],["strong",strong],["supporting",supporting]] as const)for(const field of fields){const raw=location[field];const value=Array.isArray(raw)?raw.join(" "):String(raw??"");if(terms.some(t=>new RegExp(`\\b${t.replace(/[.*+?^${}()|[\\]\\]/g,"\\$&")}\\b`,"i").test(value)))out.push({field,value,strength});}return out;}
export function evidenceConfidence(e:RoleEvidence[],specialized=true){const a=e.filter(x=>x.strength==="authoritative").length,s=e.filter(x=>x.strength==="strong").length,p=e.filter(x=>x.strength==="supporting").length;if(specialized&&!a&&s<1)return 0;if(a)return Math.min(1,.85+s*.05+p*.02);if(s>=2)return Math.min(.9,.7+s*.07+p*.02);if(s===1&&p)return .72;return specialized?0:.65;}
