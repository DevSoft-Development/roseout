import { FoundationScreen } from "@/components/FoundationScreen";

export default function ProfileScreen() {
  return (
    <FoundationScreen
      eyebrow="PROFILE"
      title="Your preferences"
      description="Account, preferences, notification settings, privacy, and app settings will live here without pulling business/admin controls into the consumer app."
    />
  );
}
