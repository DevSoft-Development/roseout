import UserDashboardShell,{DashboardCard} from "@/components/user/UserDashboardShell";import { getCurrentUserDashboardContext } from "@/lib/user-dashboard";import BetaStandaloneForm from "./StandaloneForm";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const ctx=await getCurrentUserDashboardContext();const sp=await searchParams;return <UserDashboardShell isBeta={ctx.isBeta}><DashboardCard><h1 className="text-4xl font-black">Submit Beta Feedback</h1><p className="mt-2 text-white/60">Tell us what worked, what felt off, and how the result matched your prompt.</p><BetaStandaloneForm defaults={sp}/></DashboardCard></UserDashboardShell>}
