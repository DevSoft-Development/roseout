import Link from "next/link";import CrmWorkspaceShell from "@/components/admin/crm/CrmWorkspaceShell";import {requireAdminRole} from "@/lib/admin-auth";import {CRM_WRITE_ROLES} from "@/lib/crm/permissions";import {createTaskAction} from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireAdminRole(CRM_WRITE_ROLES);const p=await searchParams;return <CrmWorkspaceShell><main className="mx-auto max-w-3xl space-y-5 text-white"><Link href="/admin/dashboard/crm/work-queue" className="text-rose-300">← Work Queue</Link><header><p className="text-xs uppercase tracking-widest text-rose-300">Operational work</p><h1 className="text-3xl font-black">Create Task</h1></header><form action={createTaskAction} className="grid gap-4 rounded-2xl border border-white/10 bg-white/[.03] p-6 sm:grid-cols-2"><label className="sm:col-span-2">Title<input name="title" required maxLength={240} className="mt-1 w-full rounded-xl bg-black/30 p-3"/></label><label className="sm:col-span-2">Description<textarea name="description" maxLength={10000} className="mt-1 min-h-28 w-full rounded-xl bg-black/30 p-3"/></label>{[["queue_key","Queue",["general","sales","outreach","claims","onboarding","support","reservations","billing","content","data_quality","renewals","partnerships"]],["task_type","Type",["follow_up","outreach","claim_review","onboarding","support","billing","reservation","site_visit","data_correction","profile_review","renewal","sales","internal","other"]],["priority","Priority",["low","normal","high","urgent"]]].map(([name,label,values]:any)=><label key={name}>{label}<select name={name} className="mt-1 w-full rounded-xl bg-black/30 p-3">{values.map((v:string)=><option key={v}>{v}</option>)}</select></label>)}<label>Due date<input type="datetime-local" name="due_at" className="mt-1 w-full rounded-xl bg-black/30 p-3"/></label><label>Team<input name="assigned_team" className="mt-1 w-full rounded-xl bg-black/30 p-3"/></label>{[["account_id","Account ID",p.account],["location_id","Location ID",p.location],["contact_id","Contact ID",p.contact],["opportunity_id","Opportunity ID",p.opportunity]].map(([n,l,v])=><label key={n}>{l}<input name={n} defaultValue={v} className="mt-1 w-full rounded-xl bg-black/30 p-3"/></label>)}<p className="sm:col-span-2 text-sm text-white/50">At least one relationship is required. Entity search is server-backed in canonical CRM pages; IDs are never preloaded in bulk.</p><button className="rounded-xl bg-rose-300 p-3 font-black text-black sm:col-span-2">Create task</button></form></main></CrmWorkspaceShell>}
