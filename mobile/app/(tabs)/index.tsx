import { FoundationScreen } from "@/components/FoundationScreen";

export default function HomeScreen() {
  return (
    <FoundationScreen
      eyebrow="THEOUTHAVEN"
      title="What are we doing?"
      description="The consumer home will become the fastest path into planning, nearby discovery, and personalized OUTings."
    />
  );
}
