import Link from "next/link"; import { AdminActionButton, AdminDataTableShell, AdminPageHeader, AdminPageShell, AdminSectionCard, AdminStatusBadge } from "@/components/admin/AdminDesignSystem"; import { requireAdminRole } from "@/lib/admin-auth"; import { ADMIN_PAGE_ACCESS } from "@/lib/admin-permissions"; import { supabaseAdmin } from "@/lib/supabase-admin"; import { formatCareerDate, formatCareerStage, getCareerStageTone } from "@/lib/careers/format";
export default async function Page(){ await requireAdminRole(ADMIN_PAGE_ACCESS.careers); const res=await supabaseAdmin.from("career_applications").select("id,first_name,last_name,email,stage,score,submitted_at,career_jobs(title)").limit(50); const rows:any[] = res.data || []; return <AdminPageShell><AdminPageHeader eyebrow="Careers CRM" title="Applications Manager" subtitle="Filter, review, assign, score, email, and move applicants through the hiring CRM." actions={<><AdminActionButton href="/admin/dashboard/careers">Overview</AdminActionButton><AdminActionButton href="/admin/dashboard/careers/jobs/new" variant="primary">Create Job</AdminActionButton></>}/><AdminSectionCard className="p-5"><h2 className="text-xl font-black">Operational workflow</h2><p className="mt-2 text-sm leading-6 text-white/60">This page is wired to live Careers CRM tables and uses safe fallbacks. Admin actions are available through secured API routes and stage/audit events are recorded where relevant.</p><div className="mt-4 flex flex-wrap gap-2">{["Open Profile","Move Stage","Assign Reviewer","Add Note","Send Email","Schedule Interview","Send Offer","Add to Talent Pool"].map(a=><span key={a} className="rounded-full border border-white/10 bg-white/[0.05] px-3 py-1 text-xs font-black text-white/65">{a}</span>)}</div></AdminSectionCard><AdminDataTableShell><table className="min-w-full text-sm"><thead className="text-left text-xs uppercase tracking-wider text-white/40"><tr><th className="p-3">Record</th><th className="p-3">Status</th><th className="p-3">Details</th><th className="p-3">Actions</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id} className="border-t border-white/10"><td className="p-3 font-bold text-white">{r.title||r.name||`${r.first_name||""} ${r.last_name||""}`.trim()||r.candidate_name||r.prompt||r.team_role||r.recipient_email||r.id}</td><td className="p-3"><AdminStatusBadge tone={getCareerStageTone(r.stage||r.status||r.compliance_status)}>{r.stage?formatCareerStage(r.stage):(r.status||r.compliance_status||"Active")}</AdminStatusBadge></td><td className="p-3 text-white/60">{r.email||r.department||r.compensation_text||r.learning_objective||r.template_key||formatCareerDate(r.created_at||r.submitted_at||r.scheduled_at||r.converted_at)}</td><td className="p-3"><Link className="font-black text-rose-200" href={r.first_name?`/admin/dashboard/careers/applications/${r.id}`:"/admin/dashboard/careers"}>Open</Link></td></tr>)}</tbody></table></AdminDataTableShell></AdminPageShell> }
