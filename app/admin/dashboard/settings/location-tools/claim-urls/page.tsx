import Link from "next/link";
import { requireAdminRole } from "@/lib/admin-auth";
import { supabaseAdmin } from "@/lib/supabase-admin";
import { LocationToolShell, ToolCard } from "@/components/admin/location-tools/LocationToolShell";
import ClaimToolsClient from "@/app/admin/dashboard/claim-tools/ClaimToolsClient";
import RepairClaimQrButton from "@/app/admin/dashboard/claim-qrs/RepairClaimQrButton";
export const dynamic="force-dynamic";
export default async function Page(){ await requireAdminRole(["superadmin","admin"]); const [missingCode, missingUrl, missingQr, claimed] = await Promise.all([supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).is("claim_code",null), supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).is("claim_url",null), supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).or("qr_code_data_url.is.null,claim_qr_url.is.null"), supabaseAdmin.from("locations").select("id",{count:"exact",head:true}).eq("is_claimed",true)]); return <LocationToolShell title="Claim URLs" description="Search, copy, regenerate, and repair claim URLs, claim codes, and QR codes." stats={[{label:"Missing claim code",value:missingCode.count||0,tone:"amber"},{label:"Missing claim URL",value:missingUrl.count||0,tone:"amber"},{label:"Missing QR",value:missingQr.count||0,tone:"rose"},{label:"Claimed",value:claimed.count||0,tone:"emerald"}]}><ToolCard title="Claim code and URL search" description="Find locations and regenerate claim credentials using the existing claim tools."><ClaimToolsClient /></ToolCard><ToolCard title="QR and claim URL repair" description="Batch repairs canonical claim URLs and regenerates missing QR code data."><RepairClaimQrButton /><div className="mt-4"><Link className="rounded-full bg-white px-5 py-3 text-sm font-black text-black" href="/admin/dashboard/claim-qrs">Open printable QR label library</Link></div></ToolCard></LocationToolShell> }
