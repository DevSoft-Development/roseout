name: Supabase Virginia schema baseline capture

on:
  push:
    branches:
      - fix/supabase-migration-history-alignment
    paths:
      - '.github/workflows/supabase-virginia-baseline-capture.yml'

permissions:
  contents: write

env:
  SUPABASE_CLI_VERSION: 2.111.0
  VIRGINIA_SUPABASE_REF: ftdsltatyqhtllyyefzp
  VIRGINIA_DB_URL: postgresql://postgres.ftdsltatyqhtllyyefzp@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require&options=-c%20jit%3don

jobs:
  capture:
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 30
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/supabase-migration-history-alignment
          fetch-depth: 0

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v2.1.1
        with:
          version: ${{ env.SUPABASE_CLI_VERSION }}
          github-token: ${{ github.token }}

      - name: Discover CLI commands
        run: |
          set -euo pipefail
          supabase --version
          supabase db dump --help >/dev/null
          supabase migration new --help >/dev/null
          command -v psql >/dev/null || { echo "psql is unavailable on the runner" >&2; exit 1; }
          command -v jq >/dev/null || { echo "jq is unavailable on the runner" >&2; exit 1; }

      - name: Inspect Virginia temporary access safely
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          test -n "$SUPABASE_ACCESS_TOKEN" || { echo "Missing SUPABASE_ACCESS_TOKEN" >&2; exit 1; }
          echo "::add-mask::$SUPABASE_ACCESS_TOKEN"

          api='https://api.supabase.com/v1/projects/'"$VIRGINIA_SUPABASE_REF"'/database'
          status_file="$RUNNER_TEMP/jit-status.json"
          mapping_file="$RUNNER_TEMP/jit-mapping.json"

          status_code="$(curl -sS -o "$status_file" -w '%{http_code}' \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            "$api/jit-access")"
          mapping_code="$(curl -sS -o "$mapping_file" -w '%{http_code}' \
            -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \
            "$api/jit")"

          echo "jit_status_http=$status_code"
          echo "jit_mapping_http=$mapping_code"
          test "$status_code" = '200' || { echo 'Unable to read Virginia JIT access status' >&2; exit 10; }
          test "$mapping_code" = '200' || { echo 'Unable to read current PAT JIT role mapping' >&2; exit 11; }

          jit_state="$(jq -r '.state // .status // .enabled // "unknown"' "$status_file")"
          role_count="$(jq '[.. | objects | .role? // empty] | unique | length' "$mapping_file")"
          roles="$(jq -r '[.. | objects | .role? // empty] | unique | join(",")' "$mapping_file")"
          has_postgres="$(jq -r '[.. | objects | .role? // empty] | any(. == "postgres")' "$mapping_file")"

          echo "jit_access_state=$jit_state"
          echo "jit_current_mapping_role_count=$role_count"
          echo "jit_current_mapping_roles=${roles:-none}"
          echo "jit_current_mapping_has_postgres=$has_postgres"

          case "$jit_state" in
            enabled|true) ;;
            *) echo 'Virginia temporary database access is not enabled; stopping before database connection.' >&2; exit 12 ;;
          esac
          test "$has_postgres" = 'true' || {
            echo 'Current GitHub Supabase PAT is not mapped to the postgres role; stopping safely.' >&2
            exit 13
          }

      - name: Verify temporary Virginia database access
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          migration_count="$(psql "$VIRGINIA_DB_URL" -X -A -t -v ON_ERROR_STOP=1 \
            -c "select count(distinct version) from supabase_migrations.schema_migrations")"
          test "$migration_count" = "282" || {
            echo "Unexpected Virginia migration ledger count: $migration_count; refusing baseline capture" >&2
            exit 3
          }
          echo "database_fingerprint=virginia"
          echo "virginia_database_target_verified=true"
          echo "temporary_database_access_verified=true"

      - name: Build replayable Virginia schema baseline
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          set -euo pipefail

          archive='supabase/migrations_archive/pre_virginia_baseline'
          mkdir -p "$archive"
          shopt -s nullglob
          migrations=(supabase/migrations/*.sql)
          test "${#migrations[@]}" -gt 0 || { echo "No active migrations found to archive" >&2; exit 1; }
          for file in "${migrations[@]}"; do
            git mv "$file" "$archive/"
          done

          supabase migration new virginia_schema_baseline
          baseline="$(find supabase/migrations -maxdepth 1 -type f -name '*_virginia_schema_baseline.sql' -print -quit)"
          test -n "$baseline" || { echo "Baseline migration file was not created" >&2; exit 1; }

          schema_dump="$RUNNER_TEMP/virginia-application-schema.sql"
          supabase db dump \
            --db-url "$VIRGINIA_DB_URL" \
            --schema public,private,fraud_internal \
            -f "$schema_dump"
          test -s "$schema_dump" || { echo "Schema dump is empty" >&2; exit 1; }

          cat > "$baseline" <<'SQL'
          -- TheOutHaven Virginia production schema baseline.
          -- Generated from the live Virginia project after the Oregon -> Virginia cutover.
          -- Historical migrations are retained under supabase/migrations_archive/ for audit only.
          -- This baseline intentionally excludes migration_work/source_* migration helper schemas,
          -- Supabase migration history, cron jobs, and production data.

          create extension if not exists "btree_gist" with schema public;
          create extension if not exists "dblink" with schema extensions;
          create extension if not exists "http" with schema extensions;
          create extension if not exists "pg_cron";
          create extension if not exists "pg_net" with schema public;
          create extension if not exists "pg_stat_statements";
          create extension if not exists "pg_trgm" with schema public;
          create extension if not exists "pgcrypto" with schema extensions;
          create extension if not exists "postgis" with schema extensions;
          create extension if not exists "postgres_fdw" with schema extensions;
          create extension if not exists "supabase_vault" with schema vault;
          create extension if not exists "unaccent" with schema public;
          create extension if not exists "uuid-ossp" with schema extensions;
          create extension if not exists "vector" with schema public;

          SQL
          cat "$schema_dump" >> "$baseline"
          cat >> "$baseline" <<'SQL'

          -- The application owns this trigger; auth.users itself remains Supabase-managed.
          drop trigger if exists on_auth_user_created on auth.users;
          create trigger on_auth_user_created
            after insert on auth.users
            for each row execute function public.handle_new_user();
          SQL

          ! grep -Eq '^COPY .* FROM stdin;' "$baseline" || {
            echo "Unexpected table data stream found in schema baseline" >&2
            exit 1
          }
          ! grep -Eq 'CREATE SCHEMA (source_|migration_work)|CREATE TABLE (source_|migration_work)' "$baseline" || {
            echo "Migration helper schema leaked into baseline" >&2
            exit 1
          }
          grep -q 'CREATE' "$baseline"
          grep -q 'public.handle_new_user' "$baseline"

          echo "baseline_path=$baseline"
          echo "archived_migration_count=${#migrations[@]}"
          echo "baseline_bytes=$(wc -c < "$baseline")"

      - name: Commit generated baseline to feature branch
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add supabase/migrations supabase/migrations_archive
          git diff --cached --quiet && { echo "No baseline changes to commit" >&2; exit 1; }
          git commit -m 'Baseline Supabase schema from Virginia production'
          git push origin HEAD:fix/supabase-migration-history-alignment
