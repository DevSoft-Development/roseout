import { requireAdminRole } from "@theouthaven/auth/admin-session";import { resumeProfileRun } from "@/lib/search/profile/profileRunRepository";import { NextResponse } from "next/server";export async function POST(_r:Request,{params}:{params:Promise<{runId:string}>}){await requireAdminRole(["superadmin", "admin"]);try{return NextResponse.json({run:await resumeProfileRun((await params).runId,false)});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Resume failed"},{status:500});}}
