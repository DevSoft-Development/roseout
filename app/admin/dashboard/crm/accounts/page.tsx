import Link from "next/link";import { requireAdminRole } from "@/lib/admin-auth";import { CRM_READ_ROLES } from "@/lib/crm/permissions";import { listAccounts } from "@/lib/crm/queries/account-summary";import { accountTypes,lifecycleStages } from "@/lib/crm/types";import { createAccountAction } from "./actions";
export const dynamic="force-dynamic";
export default async function AccountsPage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireAdminRole(CRM_READ_ROLES);const p=await searchParams;const result=await listAccounts({search:p.q,lifecycle:p.lifecycle,type:p.type,owner:p.owner,health:p.health,page:Number(p.page)||1});return <main className="mx-auto max-w-7xl space-y-6 p-6 text-white"><header><p className="text-xs font-bold uppercase tracking-widest text-rose-300">Canonical CRM</p><h1 className="text-3xl font-black">Accounts</h1><p className="text-white/60">Commercial relationships across one or many locations.</p></header><form className="grid gap-3 rounded-2xl border border-white/10 bg-white/5 p-4 md:grid-cols-4"><input name="q" defaultValue={p.q} placeholder="Search accounts" className="rounded-xl bg-black/30 p-3"/><select name="lifecycle" defaultValue={p.lifecycle||""} className="rounded-xl bg-black p-3"><option value="">All lifecycles</option>{lifecycleStages.map(x=><option key={x}>{x}</option>)}</select><select name="type" defaultValue={p.type||""} className="rounded-xl bg-black p-3"><option value="">All types</option>{accountTypes.map(x=><option key={x}>{x}</option>)}</select><button className="rounded-xl bg-rose-300 px-4 font-bold text-black">Filter</button></form><details className="rounded-2xl border border-white/10 p-4"><summary className="cursor-pointer font-bold">Create account</summary><form action={createAccountAction} className="mt-4 grid gap-3 md:grid-cols-4"><input required name="name" placeholder="Account name" className="rounded-xl bg-black/40 p-3"/><select name="account_type" className="rounded-xl bg-black p-3">{accountTypes.map(x=><option key={x}>{x}</option>)}</select><select name="lifecycle_stage" className="rounded-xl bg-black p-3">{lifecycleStages.map(x=><option key={x}>{x}</option>)}</select><button className="rounded-xl bg-white px-4 font-bold text-black">Create</button></form></details><section className="overflow-hidden rounded-2xl border border-white/10"><div className="grid grid-cols-[2fr_1fr_1fr_1fr_1fr] gap-3 bg-white/10 p-3 text-xs font-bold uppercase"><span>Account</span><span>Lifecycle</span><span>Locations</span><span>Work</span><span>Last activity</span></div>{result.accounts.length?result.accounts.map((a:any)=><Link key={a.id} href={`/admin/dashboard/crm/accounts/${a.id}`} className="grid grid-cols-[2fr_1fr_1fr_1fr_1fr] gap-3 border-t border-white/10 p-4 hover:bg-white/5"><span><b>{a.name}</b><small className="block text-white/50">{a.account_type} · {a.health_status||"health not set"}</small></span><span>{a.lifecycle_stage}</span><span>{a.crm_account_locations?.[0]?.count||0}</span><span>{a.crm_opportunities?.[0]?.count||0} opportunities · {a.crm_tasks?.[0]?.count||0} tasks</span><span>{a.last_activity_at?new Date(a.last_activity_at).toLocaleDateString():"—"}</span></Link>):<p className="p-10 text-center text-white/60">No accounts match these filters.</p>}</section><p className="text-sm text-white/50">{result.count} account{result.count===1?"":"s"} · page {result.page}</p></main>}
