# Platform Error Operations

TheOutHaven captures production application failures into `platform_error_events` and surfaces them at `/admin/dashboard/platform-errors`.

The daily Platform Error Digest sends at 6:15 AM Eastern. Critical-severity events may generate an immediate alert. Search-quality issues remain in Search Health; only true technical search failures overlap with platform error operations.
