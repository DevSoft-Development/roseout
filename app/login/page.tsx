"use client";
import { useMemo, useState } from "react";
import Link from "next/link";
import { createClient } from "@/lib/supabase-browser";
import { getZipMarketMapping } from "@/lib/zip-market-mapping";

type Tab = "signin" | "signup";
type SignupStep = 1 | 2;

type SignupState = {
  full_name: string; email: string; mobile_number: string; zip_code: string; password: string; confirm_password: string; promo_code: string;
  outing_preferences: string[]; budget_range: string; preferred_areas: string[]; venue_preferences: string[]; age_range: string; nightlife_frequency: string; interested_in_member_perks: boolean;
};
const inputClass = "min-h-[56px] w-full rounded-2xl border border-white/10 bg-white/5 px-4 text-white";
const selectClass = "min-h-[56px] w-full rounded-2xl border border-white/10 bg-white/5 px-4 text-white";
const initialSignupState: SignupState = { full_name: "", email: "", mobile_number: "", zip_code: "", password: "", confirm_password: "", promo_code: "", outing_preferences: [], budget_range: "", preferred_areas: [], venue_preferences: [], age_range: "", nightlife_frequency: "", interested_in_member_perks: false };
const passwordChecks = (p: string) => ({ minLength: p.length >= 8, uppercase: /[A-Z]/.test(p), lowercase: /[a-z]/.test(p), number: /\d/.test(p), special: /[^A-Za-z0-9]/.test(p) });

export default function LoginPage({ initialTab = "signin" }: { initialTab?: Tab }) {
 const supabase = createClient(); const [tab, setTab] = useState<Tab>(initialTab); const [showUserSignup, setShowUserSignup] = useState(initialTab === "signup"); const [signupStep, setSignupStep] = useState<SignupStep>(1);
 const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [agreeTerms, setAgreeTerms] = useState(false); const [smsOptIn, setSmsOptIn] = useState(false);
 const [signin, setSignin] = useState({ email: "", password: "" }); const [signup, setSignup] = useState<SignupState>(initialSignupState);
 const pass = useMemo(() => passwordChecks(signup.password), [signup.password]); const strong = Object.values(pass).every(Boolean); const mobileProvided = signup.mobile_number.trim().length > 0;
 const handleSignIn = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setError(""); const { error } = await supabase.auth.signInWithPassword({ email: signin.email, password: signin.password }); setLoading(false); if (error) return setError(error.message); window.location.href = "/create"; };
 const handleCreate = async (e: React.FormEvent) => { e.preventDefault(); setError(""); if (!strong) return setError("Please use a stronger password."); if (signup.password !== signup.confirm_password) return setError("Passwords do not match."); if (!agreeTerms) return setError("You must agree to the Terms of Use and Privacy Policy."); if (mobileProvided && !smsOptIn) return setError("Please agree to SMS messaging terms to receive text updates."); const derived = getZipMarketMapping(signup.zip_code); if (!derived) return setError("Please enter a valid 5-digit ZIP code."); setLoading(true);
 if (signup.promo_code.trim()) { const vr = await fetch("/api/promo-codes/validate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ code: signup.promo_code, audience: "users" }) }); const vd = await vr.json(); if (!vd.valid) { setLoading(false); return setError("This promo code is not valid or has expired."); } }
 const { data, error } = await supabase.auth.signUp({ email: signup.email.trim().toLowerCase(), password: signup.password, options: { data: { role: "user", full_name: signup.full_name.trim(), account_type: "user" } } }); if (error) { setLoading(false); return setError(error.message); }
 const userId = data.user?.id; if (!userId) { setLoading(false); return setError("Signup succeeded, but profile setup failed."); }
 const { error: profileError } = await supabase.from("user_profiles").upsert({ id: userId, full_name: signup.full_name.trim(), mobile_number: signup.mobile_number.trim() || null, zip_code: signup.zip_code, derived_city: derived.city, derived_state: derived.state, derived_market_area: derived.marketArea, sms_opt_in: mobileProvided ? smsOptIn : false, sms_opt_in_at: mobileProvided && smsOptIn ? new Date().toISOString() : null, plan: "registered", weekly_search_limit: 3 }, { onConflict: "id" });
 if (profileError) { setLoading(false); return setError(profileError.message); }
 if (signup.promo_code.trim()) { await fetch("/api/promo-codes/redeem", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ code: signup.promo_code, audience: "users" }) }); }
 setLoading(false); setMessage("Please check your email to verify your account before using TheOutHaven."); };
 return <main className="min-h-screen bg-[#090706] px-4 pb-10 pt-28 text-white"><section className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-2"><div className="rounded-[2rem] border border-white/10 bg-[#120d0b] p-8">...</div><div className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-5"><div className="mb-6 flex rounded-full border border-white/10 bg-black/40 p-1"><button onClick={() => setTab("signin")} className="flex-1 rounded-full px-4 py-2 text-sm">Sign In</button><button onClick={() => { setTab("signup"); setShowUserSignup(false); }} className="flex-1 rounded-full px-4 py-2 text-sm">Create Account</button></div>{error && <p className="mb-3 text-red-300">{error}</p>}{message && <p className="mb-3 text-emerald-300">{message}</p>}{tab === "signin" ? <form onSubmit={handleSignIn} className="space-y-3"><input required type="email" placeholder="Email" value={signin.email} onChange={(e) => setSignin((s) => ({ ...s, email: e.target.value }))} className={inputClass} /><input required type="password" placeholder="Password" value={signin.password} onChange={(e) => setSignin((s) => ({ ...s, password: e.target.value }))} className={inputClass} /><button className="w-full rounded-2xl bg-gradient-to-r from-rose-700 to-amber-500 px-4 py-4">{loading ? "Signing In..." : "Sign In"}</button></form> : !showUserSignup ? <button onClick={() => setShowUserSignup(true)} className="w-full rounded-2xl border border-rose-300/30 py-4">Create User Account</button> : <form onSubmit={handleCreate} className="space-y-4"><div className="text-xs text-white/60">Step {signupStep} of 2</div>{signupStep === 1 ? <div className="grid gap-3 sm:grid-cols-2"><input required placeholder="Full name" value={signup.full_name} onChange={(e) => setSignup((s) => ({ ...s, full_name: e.target.value }))} className="sm:col-span-2 min-h-[56px] rounded-2xl border border-white/10 bg-white/5 px-4 text-white" /><input required type="email" placeholder="Email" value={signup.email} onChange={(e) => setSignup((s) => ({ ...s, email: e.target.value }))} className="sm:col-span-2 min-h-[56px] rounded-2xl border border-white/10 bg-white/5 px-4 text-white" /><input placeholder="Mobile number" value={signup.mobile_number} onChange={(e) => setSignup((s) => ({ ...s, mobile_number: e.target.value }))} className={inputClass} /><input required placeholder="ZIP code" value={signup.zip_code} onChange={(e) => setSignup((s) => ({ ...s, zip_code: e.target.value }))} className={inputClass} /><input required type="password" placeholder="Password" value={signup.password} onChange={(e) => setSignup((s) => ({ ...s, password: e.target.value }))} className={inputClass} /><input required type="password" placeholder="Confirm password" value={signup.confirm_password} onChange={(e) => setSignup((s) => ({ ...s, confirm_password: e.target.value }))} className={inputClass} /><input placeholder="Promo code" value={signup.promo_code} onChange={(e) => setSignup((s) => ({ ...s, promo_code: e.target.value }))} className="sm:col-span-2 min-h-[56px] rounded-2xl border border-white/10 bg-white/5 px-4 text-white" />{Object.entries(pass).map(([k, v]) => <p key={k} className={v ? "text-emerald-300 text-xs sm:col-span-2" : "text-white/50 text-xs sm:col-span-2"}>{k}</p>)}</div> : <div className="space-y-4"><select className={selectClass}><option>Typical outing budget</option></select><select className={selectClass}><option>Age range</option></select><select className={selectClass}><option>How often do you go out</option></select></div>}<label className="block text-sm"><input type="checkbox" checked={agreeTerms} onChange={(e) => setAgreeTerms(e.target.checked)} className="mr-2" />I agree to the <Link href="/terms" className="underline">Terms</Link> and <Link href="/privacy" className="underline">Privacy</Link>.</label><label className="block text-sm"><input type="checkbox" checked={smsOptIn} onChange={(e) => setSmsOptIn(e.target.checked)} className="mr-2" />SMS opt-in compliance checkbox.</label><div className="flex gap-2">{signupStep === 2 && <button type="button" onClick={() => setSignupStep(1)} className="flex-1 rounded-2xl border border-white/20 py-3">Back</button>}{signupStep === 1 ? <button type="button" onClick={() => setSignupStep(2)} className="flex-1 rounded-2xl bg-gradient-to-r from-rose-700 to-amber-500 py-3">Continue</button> : <button disabled={loading} className="flex-1 rounded-2xl bg-gradient-to-r from-rose-700 to-amber-500 py-3">{loading ? "Creating..." : "Create Account"}</button>}</div></form>}</div></section></main>;
}
